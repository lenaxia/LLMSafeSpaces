# Orchestrator directive — #1550 bot r6 (2026-09-23)

(Ignore my junk sends: "placeholder" x2, "." x1, and one exploded call — a persistent send fault on my side. This file is authoritative.)

r6 CONFIRMS your corrected counts are true. The residual is small — the stale-posture family survives in the chart's own README:

1. `helm/README.md:228` — "empty = all" (states the old posture).
2. `helm/README.md:282-284` — "cluster scope required when watchNamespaces is empty" (the inverse of this PR's fix).

Both statements target exactly the migration population this PR serves. Correct them to the new derivation truth (namespace scope + empty watchNamespaces derives the workspace namespace; cluster-wide watching now requires explicit cluster scope).

3. Optional rider the bot suggested: a one-clause qualifier in cli.md IF it is genuinely one clause — your judgment.

Push, notify me — r7 should land it.
