# Trivy ignore file for LLMSafeSpaces.
#
# Format: each line is either:
#   - a CVE ID (e.g. CVE-2024-1234)
#   - a misconfig ID (e.g. KSV-0014)
#   - a CVE ID followed by `exp:YYYY-MM-DD` (expiration)
#
# Path-scoped ignores live in .trivyignore.yaml (separate file). This
# file is for IDs we accept globally with documented rationale.
#
# Reviewed quarterly: any expired ignores are re-triaged.

# -----------------------------------------------------------------------------
# Misconfigurations: development manifests in local/ and security audit
# fixtures in design/stories/epic-17-security-review/phase-0*/ deliberately
# ship with weak security context to exercise specific scenarios. Production
# install path is the chart in charts/llmsafespaces/ which is hardened.
#
# We do NOT use a global ignore; instead .trivyignore.yaml scopes by path.
# Keep this file for global CVE accepts only.

# CVE-2026-59869 — js-yaml: DoS via crafted YAML
# Transitive npm dev dependency used by build tooling (eslint, vitest,
# js-yaml CLI). Not shipped in any production image — the frontend
# Dockerfile runs npm ci and copies only the built dist/ into nginx.
# Re-evaluate when npm-lock.json is updated.
CVE-2026-59869 exp:2026-10-21

# GHSA-qwww-vcr4-c8h2 — react-router: RSC Mode CSRF Bypass
# Not exploitable: this advisory affects React Server Components (RSC) mode
# with server actions. LLMSafeSpaces uses client-side routing only
# (createBrowserRouter — no SSR, no server actions, no RSC). The vulnerable
# code path (action execution before 400 response) requires an RSC server
# runtime that this codebase does not run. Affects react-router 7.12.0-8.2.0;
# no patched release exists yet (downgrade to 7.11.0 is the only npm-audit
# suggestion, which would lose bug fixes). Re-evaluate when a patched
# react-router release ships.
GHSA-qwww-vcr4-c8h2 exp:2026-10-21

# KSV-0053 — pods/exec writable by the API's terminal Role.
# Unmasked 2026-08-15 when the Trivy helm scanner finally rendered the
# chart (--helm-kube-version matched the >=1.35 floor; before that the
# render failed silently and the rule never ran). The terminal feature
# requires pods/exec create; RBAC cannot scope subresources by
# resourceNames/labels, so the compensating control is the runtime label
# check in api/internal/handlers/terminal.go (F1.3.6, documented inline
# in helm/templates/rbac.yaml). Accepted with that caveat.
KSV-0053

# KSV-0056 — controller Role writes networkpolicies.
# Same unmasking as KSV-0053. The controller's per-workspace egress
# NetworkPolicy generator (F1.2.4/G4) is the legitimate primary user of
# networkpolicies CRUD in the workspace namespace; this is the feature,
# not an over-grant. The rule also lists services/endpoints/ingresses
# which we do NOT grant (F1.3.1 removed them). Accepted.
KSV-0056

# DS-0002 — no USER in cmd/workspace-agentd/Dockerfile.
# The image is never executed: it exists to be mounted as a read-only
# image volume (KEP-4639) in workspace pods, and its single file is
# verified by sha256 before exec from the workspace container (which
# runs as its own non-root user per pod securityContext). FROM scratch
# has no passwd database to name a user with; a USER directive would be
# cargo cult. Re-triage if this image ever gains an entrypoint.
DS-0002 exp:2027-08-15
