# Worklog: US-72.0 / #910 — generalized relay re-arm loop (bounded backoff, busy-gated, per-cycle outcome)

**Date:** 2026-09-16
**Session:** Implement epic-72 US-72.0 (= issue #910): re-triggerable relay injector — bounded backoff 5m→30m cap, gated on no busy sessions, `HasRelay()` re-check per cycle, ctx-alive, per-outcome metric — landing the generalized "terminal fetch failure → loud degrade → bounded re-arm" loop US-72.4 consumes.
**Status:** Complete — implementation session appended below (the design note above is the pre-implementation artifact from commit `ac4f2fcc`, unchanged)

---

## Design note (written BEFORE implementation — per #910 "Design note in worklog before implementation")

### D1 — The generalized loop is its own component; the injector is its first consumer

New file `cmd/workspace-agentd/rearm_loop.go`: `startRearmLoop(ctx, rearmLoopConfig)` — a single goroutine that
waits → gates → attempts → records outcome → re-arms. Gates are closures (`Applied`, `Busy`, `RestartDeferred`),
the work is an `Attempt(ctx) rearmAttemptResult` closure (`{outcome string, retryable, applied bool}`). No
injector-specific knowledge in the loop; no loop-specific knowledge in the attempt. US-72.4 (agentd relay-only
liveness/degrade codes) consumes the same loop with its own Attempt/gates — the story names this consumer, so the
seam is justified under Rule 12 (a funded second consumer, not speculation). It lives in `cmd/workspace-agentd`
(both consumers are agentd-internal; promoting to `pkg/` would require an exported API surface before the second
consumer's shape is known).

### D2 — Backoff schedule

Waits double from `MinDelay` (default 5m) capped at `MaxDelay` (default 30m): 5m → 10m → 20m → 30m → 30m …
(the #910-proposed schedule). Only a FAILED attempt advances the delay. A gate skip (busy / restart-deferred /
already-applied) neither advances nor resets it — a skip is not a failure. Terminal exits: attempt applied
(success → disarm), attempt non-retryable (personal key → permanent disarm), `Applied()` true at cycle entry
(`HasRelay()` re-check → disarm), ctx canceled (stop). Exactly one in-flight re-arm by construction: the loop is
one goroutine; `startRelayInjector` is called once per agentd process.

### D3 — Outcome taxonomy and metrics

The attempt returns the injector's EXISTING outcome strings (`fetch_failed`, `no_free_models`,
`unhealthy_timeout`, `config_write_failed`, `auth_write_failed`, `skipped_personal_key`, `success`,
`success_no_restart`), plus loop-level outcomes (`already_applied`, `busy`, `restart_deferred`, `canceled`).
Boot-window ticks stay in `llmsafespaces_relay_injector_total{outcome}` (unchanged label set, existing
dashboards/tests untouched); re-arm cycles tick the new generalized vec
`llmsafespaces_rearm_outcome_total{loop,outcome}` with `loop="relay_injector"` — the design's `relay_rearm_outcome`
metric realized as this loop's series of a generic vec (a relay-specific metric name would be wrong for US-72.4's
consumer). Exactly one tick per cycle with the cycle's final outcome.

### D4 — Kill-deferral interplay (#910's named constraint: attempts must not stack behind deferred kills)

A deferred restart implies sessions were busy at defer time — the re-arm `Busy` gate (the existing
`sessionStatusTracker` via `trackerHasBusyOrUnknown`) covers the steady state. The remaining window is the
≤`restartIdleCheckInterval` (5s) between the last busy→idle transition and the deferred goroutine's restart tick:
a re-arm attempt landing there would stack a second restart behind the pending one. Closed with a new
`deferredRestarts` atomic counter incremented/decremented around the deferred-restart goroutine in
`makeSessionAwareRestartDecision` (all call sites — credential apply, relay kill, XDG watcher — benefit), read by
the loop's `RestartDeferred` gate: skip the cycle (outcome `restart_deferred`) while any deferral is outstanding.
A successful re-arm's kill reuses `relayKillFunc` unchanged — i.e. the #1374 session-aware decision (unbounded
defer while progressing, interrupt-first force path) composes as-is. The re-arm loop disarms on success (the
writer's relay state is set before the kill), so the loop itself never re-fires behind its own kill.

### D5 — `HasRelay()` short-circuit and readyz truthfulness

Preserved twice: the boot-time Phase-D short-circuit (pre-boot applied → no injection at all) is untouched, and
the re-arm loop re-checks `HasRelay()` at every cycle entry (`already_applied` → disarm) — covering relay applied
by any other path mid-pod-life. readyz `RelayInjected` continues to evaluate `HasRelay()` live per request —
truthful per cycle by construction; pinned by test.

### D6 — Retryability classes

Retryable (re-arm): `fetch_failed`, `no_free_models`, `unhealthy_timeout`, `config_write_failed`,
`auth_write_failed`. Permanent (disarm): `skipped_personal_key` (personal Zen key = relay correctly bypassed).
Known residual (pre-existing, documented): Apply success + auth.json write failure leaves `HasRelay()` true with a
missing auth entry; the next cycle disarms via `already_applied` — same terminal state as the one-shot injector.

### D7 — Context and lifecycle

The loop lives under the injector's existing `rootCtx` (SIGTERM-scoped). Cancel while waiting → one `canceled`
tick + exit; cancel during an attempt → the attempt's inner fetch loops return `canceled` → tick + exit. Each
re-arm cycle runs one full bounded fetch attempt (same `FetchDeadline` as boot, default 30s).

### D8 — statusz `relayFreeModelsState` (#901 G8) semantics under re-arm

State stays 2 (degraded) while re-arm cycles fail; the first successful cycle flips it to 1. The stale-window
wording ("degraded until the next agent restart") is deleted wherever it appears — recovery is now bounded by the
backoff, and README-LLM.md's relay-config section is updated where this change makes it stale (Rule 5).

### Assumptions (stated; validation recorded at bottom — Rule 7)

| # | Assumption | Validation plan |
|---|---|---|
| A1 | `relayInjectorOutcomes` label set must stay boot-only (rearm in its own vec) so existing delta tests/dashboards stay hermetic | existing tests assert deltas on shared package counters (relay_injector_test.go:587) |
| A2 | `HasRelay()` is true exactly when the writer holds relay state (staged via Apply or loaded from disk) | `pkg/agent/opencode/configwriter.go` + TestStartRelayInjector_SkipsWhenPreBootApplied |
| A3 | a deferred restart ⇒ busy at defer; Busy gate covers steady state, `deferredRestarts` covers the ≤5s poll window | `makeSessionAwareRestartDecision` (secrets.go) read; pinned by test |
| A4 | `startRelayInjector` is called exactly once per agentd process | main.go single call site |
| A5 | 5m→30m defaults | #910 issue text ("e.g. 5m → 30m cap") — author direction |

---

## Work Completed

Implementation session (2026-09-16, resumed after an interrupted first pass — assessment and
repairs recorded under Key Decisions K1–K2):

- `cmd/workspace-agentd/rearm_loop.go` (NEW): the generalized loop per D1–D3 — `startRearmLoop(ctx, cfg)`
  spawns one goroutine running `runRearmLoop`: wait(backoff) → `Applied()` short-circuit → `Busy()` gate →
  `RestartDeferred()` gate → `Attempt(ctx)` → exactly one `llmsafespaces_rearm_outcome_total{loop,outcome}` tick →
  advance delay only on retryable failure. Terminal exits: applied, non-retryable, already-applied, canceled.
- `cmd/workspace-agentd/relay_injector.go`: the old inline boot body is extracted into
  `relayInjectorConfig.attempt(ctx, lg, healthWait) rearmAttemptResult` — the shared body of the boot window
  (healthWait=5m) and every re-arm cycle (healthWait=0: probe once; mid-pod-life unhealthy is a retryable
  outcome, not a block). Outcome strings promoted to named constants (same values — dashboards/delta tests
  unchanged). `startRelayInjector` runs the boot attempt once, ticks
  `llmsafespaces_relay_injector_total{outcome}` (boot only, per A1), then hands the SAME attempt to
  `startRearmLoop` on terminal retryable failure. `relayFreeModelsState` per D8: 2 while cycles fail, 1 on the
  applying cycle. New config knobs: `Busy`, `RestartDeferred`, `RearmMinDelay`, `RearmMaxDelay`.
- `cmd/workspace-agentd/secrets.go`: `deferredRestarts atomic.Int64` gauge around the deferred-restart
  goroutine in `makeSessionAwareRestartDecision` (`Add(1)` at spawn; `defer Add(-1)` first line of the
  goroutine body — clears on restart-fired, force-path, and shutdown-cancel exits) + `anyRestartDeferred()`.
  The immediate (non-deferred) restart path never touches the gauge.
- `cmd/workspace-agentd/main.go`: `maybeStartRelayInjector` wires `Busy: trackerHasBusyOrUnknown(sseTracker)`
  (the same oracle the restart deferral trusts) and `RestartDeferred: anyRestartDeferred`.
- `cmd/workspace-agentd/rearm_loop_test.go` (NEW) and `relay_rearm_test.go` (NEW): the full story test matrix
  (see Tests Run).
- `README-LLM.md` §Relay Config Subsystem (Rule 5): fragility #2 rewritten (fetch-failure class resolved by
  #910, credential-change class still open); boot write-sequence step 4 mentions the re-arm; the RelayInjected
  stale-window note covers the mid-pod-life apply case.

---

## Key Decisions

Design decisions D1–D8 above stand as written. Session additions:

- **K1 (repair of interrupted work): `startRearmLoop` must spawn, not block.** The interrupted first pass
  left `startRearmLoop` synchronous ("callers spawn it"), which happened to work for the injector (it called
  the loop inside its own goroutine) but deadlocked every direct loop test — the tests, as written red-first,
  specify an async `start...`. Fixed by moving the body to `runRearmLoop` and spawning it in
  `startRearmLoop`; logger captured at call-site (same discipline as `startRelayInjector`). This is also the
  better US-72.4 seam: consumers call `startRearmLoop` once and never wrap it themselves. Single-in-flight
  property unchanged (still one goroutine per loop).
- **K2 (repair of interrupted work): the readyz truthfulness test never healed its fault server** — it waited
  for `HasRelay()` with the provider stub still in fail-500 mode. Fixed by healing the stub after the
  pre-apply readyz assertion; the test now proves false-before / true-after across the re-arm apply.
- **K3: `unhealthy_timeout` is retryable.** The old boot path returned silently on unhealthy-timeout; under
  #910 a pod whose opencode took >5m to get healthy would previously lose relay forever — now it re-arms
  (`relayFreeModelsState`=2 surfaced meanwhile). Consistent with D6.
- **K4: boot-outcome metric hermeticity preserved (A1 validated).** Boot ticks land only in the existing
  injector vec; re-arm cycle ticks only in the new rearm vec; the two share the outcome-string taxonomy so
  the views stay comparable. `TestRelayRearm_BootSuccess_NeverEntersRearm` pins the no-rearm-on-success side.
- **K5: gauge placement.** `deferredRestarts.Add(1)` sits immediately before the goroutine spawn (no early
  return between), `Add(-1)` is the goroutine's first defer — every exit path clears it;
  `TestAnyRestartDeferred_GaugeLifecycle` pins fired/canceled lifecycle.

---

## Tests Run

Red-first: the loop state-machine tests and the injector re-arm tests were written against the not-yet-working
loop (and indeed deadlocked/failed on the interrupted implementation — K1/K2 — before the repairs).

- `TestNextRearmDelay_DoublingSchedule`, `TestNextRearmDelay_CapBelowDoubling` — the #910 schedule
  (5m→10m→20m→30m→30m) and clamping, pure.
- Loop state machine: `TestRearmLoop_TerminalFailure_BoundedBackoff` (cadence slows per doubling; exactly one
  in-flight attempt; one tick per cycle), `TestRearmLoop_SuccessDisarms`, `TestRearmLoop_CtxCancelStops`,
  `TestRearmLoop_CtxCanceledAttemptExits`, `TestRearmLoop_AlreadyAppliedShortCircuit`,
  `TestRearmLoop_BusyGateSkipsWithoutAttempt`, `TestRearmLoop_BusySkipDoesNotAdvanceBackoff` (a skip is not a
  failure — gap stays ~2x base), `TestRearmLoop_RestartDeferredGateSkips`, `TestRearmLoop_NonRetryableDisarms`,
  `TestRearmLoop_DefaultsFillWhenZero` (5m/30m defaults).
- Injector as first consumer (fault-injection): `TestRelayRearm_BootFetchFailure_RearmsAndApplies` (source
  down through boot, heals mid-pod-life → applied + one restart + state 1 + per-cycle ticks; the story's
  router-down-T+0/up-T+3m row, time-scaled), `TestRelayRearm_Incident20260816_Regression` (the 2026-08-16
  workspace-946a442f class: truncated `/provider` body → "unexpected EOF" decode through boot → degraded, then
  bounded recovery, exactly one restart, strictly sequential fetches), `TestRelayRearm_BusyGate_NoAttemptWhileBusy`,
  `TestRelayRearm_NoStackBehindDeferredKill` (real `makeSessionAwareRestartDecision` defer held open across
  re-arm cycles: zero re-arm fetches while deferred, applies after the deferral clears),
  `TestAnyRestartDeferred_GaugeLifecycle`, `TestRelayRearm_HasRelayShortCircuit_PreservedOnceApplied`
  (disarm-after-applied + already-applied-mid-rearm), `TestRelayRearm_BootSuccess_NeverEntersRearm`,
  `TestRelayReadyz_RelayInjected_TruthfulPerCycle` (K2).
- `-race` on the whole package: `go test -race -count=1 ./cmd/workspace-agentd/` → ok (305s).
- Repo gates: `go build ./...`, `make test`, `make lint` — all green (see PR evidence).

Assumption validations (Rule 7): A1 validated by the untouched existing delta tests plus K4's pin; A2 held
(configwriter `HasRelay()` semantics unchanged, existing tests green); A3 validated by
`TestRelayRearm_NoStackBehindDeferredKill` + K5; A4 re-verified (single `startRelayInjector` call site in
main.go); A5 as directed by the issue text.

---

## Review iteration 1 (AI reviewer, PR #1401)

CHANGES_REQUESTED on `5f42fc8c`. Every finding triaged (Rule 11 Phase 2); outcomes:

**Accepted + fixed (with pins):**
- R-C1 shutdown-window race: no ctx re-check between fetch success and Apply → auth → kill. Fixed with two
  guards in `attempt` (post-fetch, pre-kill); pinned by `TestRelayAttempt_CtxCanceledAfterFetch_NoApplyNoKill`
  (cancel mid-flight → `canceled`, no config write, no restart trigger).
- R-C2 "never stacked" held only at cycle entry: a deferral appearing during the ≤FetchDeadline attempt window
  stacked a second deferred restart. Fixed with a SECOND checkpoint — the pre-kill `RestartDeferred()` re-check
  skips the relay kill entirely (the outstanding deferred restart reads the just-written config; the attempt
  still counts applied). Pinned by `TestRelayRearm_PreKillDeferredCheck_SkipsRelayKill`. Comments/README
  softened to the two-checkpoint truth (residual gauge-check→defer-spawn TOCTOU noted as microscopic).
- R-C3 `auth_write_failed` retryable bought a guaranteed wasted cycle (Apply already set relay state → next
  cycle disarms `already_applied` without retrying the auth write). Reclassified terminal; pinned in the new
  decision-table test.
- R-T1 the outcome→`{retryable, applied}` mapping was pinned for only 2 of 9 outcomes. Added
  `TestRelayAttempt_OutcomeRetryabilityMatrix` — table-driven, real attempt body, all 8 rows (incl. the
  K3-advertised `unhealthy_timeout` retryability and the `writer_nil` terminal).
- R-T2 covered by the matrix's `auth_write_failed` row (classification fixed per R-C3 and pinned).
- R-T3 personal-key bypass is per-cycle, not boot-cached: added
  `TestRelayRearm_PersonalKeyAddedMidLife_Disarms`.
- R-R3 pre-existing failure-path tests now spawn 5m re-arm goroutines on `context.Background()`. Fixed with
  `rearmTestCtx(t)` (cancelable, t.Cleanup) in the four failure-path tests.
- R-P1 stale docs D8 missed: `pkg/agentd/types.go` `RelayFreeModels` ("until the next agent restart") and
  `RelayInjected` ("one-time boot event") updated; `helm/templates/prometheus-rules.yaml`
  `LLMSafeSpacesRelayInjectorDegraded` description rewritten re-arm-aware and now points operators at
  `llmsafespaces_rearm_outcome_total` for recovery vs persistent failure.
- R-S1 dead `rearmOutcomeApplied` const removed (the applied cycle ticks the consumer's success taxonomy;
  comment explains why no loop-level applied string exists).

**Declined (false alarm / already covered):**
- R-T4 (wiring pass-through of Busy/RestartDeferred to the loop): already exercised —
  `TestRelayRearm_BusyGate_NoAttemptWhileBusy` and `TestRelayRearm_NoStackBehindDeferredKill` drive the
  PRODUCTION wiring shape (gates injected via the same harness path main.go uses) and would fail on nil-gate
  regression (no busy/restart_deferred ticks). No new test needed.
- Alert follow-up (persistent re-arm failure paging): the description rewrite covers operator guidance; a
  dedicated `rearm` alert rule (e.g. absent-success over N cycles) needs threshold/validation work — recorded
  as the natural US-72.4 companion when its liveness loop lands on the same vec, not silently invented here.

**Correction to iteration-1 claims:** "boot vec labels unchanged" was imprecise — `writer_nil` is a NEW
(additive, harmless) label value on `llmsafespaces_relay_injector_total`; existing values unchanged. D8's
"deleted wherever it appears" is now actually true (types.go + alert were the misses, fixed above).

## Review iteration 2 (AI reviewer, PR #1401)

CHANGES_REQUESTED on `3b8ac6d8`. Triage:

- R2-C1 promtool fixture BLOCKER — VALID, fixed: the rewritten `LLMSafeSpacesRelayInjectorDegraded`
  description was not propagated to `helm/tests/alerts_promtool_test.yaml` (both rows). Promtool is absent
  locally (the test skips), which is why local gates were green while CI failed deterministically. Both
  `exp_annotations.description` entries now match the rendered template exactly (verified by folding the
  template's block scalar and comparing strings).
- R2-C2 flaky pre-kill pin BLOCKER — VALID, already fixed in `e4941c47` (delta baseline on the success-tick
  Eventually + the tick is the post-`Store(1)` ordering signal: it is emitted by the loop only after the
  attempt returns, and the attempt stores state 1 before returning). The same commit swept every other
  rearm-vec assertion to delta form (`-count=5`, `-race -count=3` green).
- R2-C3 weak ctx-guard discriminator — VALID, mitigated + acknowledged: the test now writes the full valid
  body BEFORE canceling (the expected path completes the fetch against the dead ctx so the post-fetch guard
  fires; on pre-fix code that path applies+kills, making the pin a real discriminator). The comment documents
  the transport-race fallback (body read aborted → canceled via the retry loop's ctx select — same terminal
  outcome, so the pin cannot flake post-fix).
- R2-P1 `RelayInjected` doc overclaim — VALID, fixed: the types.go comment now says true = writer holds relay
  state (applied), names the pending-restart corners (session-aware deferral; pre-kill skip behind an
  outstanding deferred restart; terminal auth-write-failure residual), and drops the awkward phrasing.
- R2-P2 worklog corrections — accepted: iteration-1's "all green" was a local-artifact claim (promtool absent,
  flake un-reproduced) — the record above now states this; D8's "wherever it appears" corollary is satisfied
  only as of the fixture fix in this iteration.

## Review iteration 3 (AI reviewer, PR #1401)

CHANGES_REQUESTED — but the review's own header reads "**Commit reviewed:** `e4941c47`": the review process
snapshotted the PREVIOUS head mid-push, so all four findings were already fixed by `c426988f` at the moment
the review published. Triage:

- R3-C1 promtool fixture (carried) — fixed in `c426988f` (see iteration-2 section above; fixture rows verified
  string-equal to the rendered template).
- R3-C2 ctx-guard discriminator (carried) — fixed in `c426988f` (body-before-cancel ordering + limitation
  comment).
- R3-C3 `RelayInjected` doc overclaim (carried) — fixed in `c426988f`.
- R3-P2 worklog inconsistencies — the "iteration 2 pending" placeholder and the too-broad green-gates claim
  were real at `e4941c47`; both corrected by `c426988f`'s worklog edit and this section.
- Frontend check: the one Playwright failure (`attachments > Composer: attaches files in the prompt payload`,
  146 passed / 1 failed) is a frontend e2e flake — this branch touches zero frontend files (agentd Go + helm
  rule text + docs). Failed-job rerun issued.

## Review iteration 4 (AI reviewer, PR #1401)

Pending — section completed when the review lands.

---

## Tests Run (iteration deltas)

- New: `TestRelayAttempt_OutcomeRetryabilityMatrix` (+8 rows), `TestRelayRearm_PersonalKeyAddedMidLife_Disarms`,
  `TestRelayRearm_PreKillDeferredCheck_SkipsRelayKill`, `TestRelayAttempt_CtxCanceledAfterFetch_NoApplyNoKill`.
- Modified: the four pre-existing failure-path injector tests now cancel their re-arm goroutines
  (`rearmTestCtx`).
- CI iteration-2 catch (full-suite job): `TestRelayRearm_PreKillDeferredCheck_SkipsRelayKill` failed — its
  success-tick Eventually lacked a delta baseline on the shared vec, so it passed on an earlier test's
  accumulated tick and the asserts raced the still-in-flight attempt (state read as 2; cleanup then yanked the
  TempDir mid-auth-write). Root-caused and fixed, then swept the WHOLE matrix for the same class: delta
  baselines added to every rearm-vec assertion in `rearm_loop_test.go` and `relay_rearm_test.go` (the vec
  accumulates across tests AND across `-count` reruns — the suite now passes `-count=5` and `-race -count=3`).
- Re-run gates after fixes: `go build ./...`, `make test`, `make lint`, `go test -race -count=1
  ./cmd/workspace-agentd/` — all green.

---

## Files Modified

Iteration 1 + review fixes:

- `cmd/workspace-agentd/rearm_loop.go` (NEW)
- `cmd/workspace-agentd/rearm_loop_test.go` (NEW)
- `cmd/workspace-agentd/relay_rearm_test.go` (NEW)
- `cmd/workspace-agentd/relay_injector.go` (attempt extraction + re-arm handoff)
- `cmd/workspace-agentd/secrets.go` (deferredRestarts gauge)
- `cmd/workspace-agentd/main.go` (Busy/RestartDeferred wiring)
- `cmd/workspace-agentd/relay_injector_test.go` (rearmTestCtx on failure-path tests)
- `pkg/agentd/types.go` (statusz/readyz doc comments, review R-P1)
- `helm/templates/prometheus-rules.yaml` (alert description, review R-P1)
- `README-LLM.md` (§Relay Config Subsystem, Rule 5)
- this worklog
