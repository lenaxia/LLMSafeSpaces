# Worklog: rbac namespace-scope cache-scoping derivation — the drill-shape crashloop fix (run 35872827066)

**Date:** 2026-09-23
**Session:** Standby assignment from the orchestrator after the monitor's report: the nightly's drill-shape pre-step (its FIRST-EVER execution — every prior nightly died upstream, so the hardened lane from #1542 carried it here for the first time) failed `helm upgrade --wait` with a 10m `context deadline exceeded`. Diagnosis first (no rerun, no push), then GO on the fix PR.
**Status:** Complete

---

## Objective

Make the runbook's own prescribed `rbac.scope=cluster → namespace` migration path actually work: under namespace scope the controller must not run a cluster-wide manager cache against namespaced RBAC.

## Work Completed

### The diagnosis (reported with evidence before any code)

- **Pod status at the deadline:** `llmsafespaces-controller 0/1 CrashLoopBackOff, 3 restarts in 10m` — the sole unready resource; everything else (api ×2, five workspaces, mock-llm, postgres, valkey) healthy. helm `--wait` burned its full 10m on that one deployment.
- **Controller logs (the failure dump):** `Could not wait for Cache to sync` (workspace controller, `kind source: *v1.ServiceAccount`) + `failed waiting for *v1.Secret Informer to sync` → manager exit → crashloop.
- **The causal chain, verified in code:** `values.yaml controller.watchNamespaces: ""` → `controller/main.go` builds a CLUSTER-WIDE cache when the flag is absent ("watching all namespaces") → the drill-shape flip deletes the ClusterRole/Binding (namespace Role covers one namespace) → `SetupWithManager`'s informers (For(Workspace) + Owns(Pod/Secret/ServiceAccount/PVC)) need list/watch across ALL namespaces → Forbidden outside the workspace namespace → informer sync never completes → exit(1) → CrashLoopBackOff.
- **Not relay-specific, not #1537:** the informer RBAC deficit is flag-independent; the relay startup guard PASSED (llm-relay reads ride the direct API reader by design §4.3 — the design's no-informer posture held while the informers died). Latent chart bug: the G5 default posture (namespace scope) itself carries it; nothing ever exercised the namespace-scope RUNTIME until the hardened lane reached the drill-shape step.

### The fix (TDD)

- **RED first:** `helm/rbac_cache_scope_chart_test.go` — the two derivation pins failed against the unfixed chart (no `--watch-namespaces` arg rendered).
- **The derivation** (`controller-deployment.yaml`): `controller.watchNamespaces` set → passes through verbatim (unchanged); unset AND namespace scope → `--watch-namespaces={{ workspaceNamespace helper }}` (the release namespace by default, `api.config.kubernetes.namespace` override honored — the helper's contract); cluster scope → NO arg (byte-identical off-path).
- **Five pins:** the derivation; the custom-namespace override; explicit-wins; cluster-scope-absent (the off-path regression pin, the repo's flag-landing convention); explicit-under-cluster-scope (pre-existing behavior unchanged).
- **Docs:** `values.yaml` comment + the `helm-values.md` reference row + a runbook "Known interactions" entry citing the full crash chain (the migration's first exercise = the nightly's exact signature) and noting the guard/direct-reader distinction.

Two template-assembly defects caught by the red/green cycle itself: Go templates do not chain `else if` on `with` (restructured as `if/else if`), and a both-side-trimmed inline comment glued the emitted arg onto the neighbor line (the tests' arg extraction returned "" — the comment moved above the conditional).

## Key Decisions

1. **Chart-level derivation, not a pre-step flag** (the orchestrator's ruling): the symptom fix would leave every operator's real migration broken; the derivation fixes the default posture (G5) and the runbook path everywhere.
2. **Explicit `watchNamespaces` always wins** — the derivation is a default, never a clobber; cluster scope renders byte-identically (pinned).
3. **The derivation follows the `workspaceNamespace` helper** (not `.Release.Namespace` directly) — one namespace-resolution point, honoring the `api.config.kubernetes.namespace` override.

## Blockers

None. This PR unblocks the #820 evidence checklist (the drill-shape step is the gate to rows 2-4: the drill run, the CredentialsStaged observation, the sweep run).

## Tests Run

- `go test ./helm/ -run 'TestRBAC' -count=1 -v` — 5/5 (grown red-first: the two derivation pins failed pre-fix).
- `go test ./helm/ -count=1` — FULL package green, 30.0s with real helm v3.16.4 (the #1534 lesson: default-posture template changes get checked against every sibling pin, not a scoped run).
- `make repolint` — all checks passed; `golangci-lint run ./helm/...` — 0 issues.

## Next Steps

1. Merge → the orchestrator's schedule-vs-manual decision for the evidence run (monitor r3 armed); the drill-shape step should now converge and the drill + sweep execute for the first time.
2. If the next run still crashloops: the derivation emitted but something else denies informer sync — the failure dump's controller logs will say which Kind; the chart's Role coverage is the next suspect (not expected: the Role grants list/watch on all five informer kinds in the workspace namespace).

## Files Modified

*(r8 refresh — the r0 list no longer matched the accumulated diff.)*

`helm/templates/controller-deployment.yaml` (the derivation + fail guard + comments), `helm/templates/rbac.yaml` (header block: storageclasses attribution, the all-namespaces qualification, the reads/writes truth; the cluster-grant intro), `helm/templates/NOTES.txt` (the workspace-namespace scope statement), `helm/rbac_cache_scope_chart_test.go` (new — 10 pin functions; the honest count, corrected r4 from "8"), `helm/values.yaml` (watchNamespaces comment + derivation note + split-ns caveat + the rbac.scope block's storageclasses/derivation corrections + the namespace-block note), `helm/README.md` (values row + RBAC-scope section), `docs/reference/helm-values.md` (two rows), `docs/reference/cli.md` (the --watch-namespaces flag row), `docs/operator/multi-tenant.md` (two prescription sites + the scope table), `docs/runbooks/relay-only-flip.md` (Known-interactions entry), this worklog.

## r1 — the watch-all hole, the contradicting bullets, the split-ns caveat (bot review)

1. **`watchNamespaces: "*"` under namespace scope crashlooped identically** (the migration population's own spelling): the template's truthy check passed `*` through verbatim → the controller parses it to a nil namespace map → cluster-wide cache vs namespaced RBAC → the exact run-35872827066 death. Fixed per the remediation ruling: a render-time `fail` (the llm-relay-rbac fail-loud convention in the same file) for `"*"` AND whitespace-only values under namespace scope, the message naming both remedies (explicit namespaces, or keep `rbac.scope=cluster`). Pinned by `TestRBACNamespaceScope_WatchAllFailsRender` (3 value shapes × 4 message assertions) + `TestRBACClusterScope_WatchAllStillValid` (`"*"` stays VALID under cluster scope — the guard must not leak across the scope boundary). MUTATION-VERIFIED: disabling the guard turns the pin 4-red. My first guard's whitespace branch compared the already-trimmed value against empty (unreachable — the pin's whitespace subtests caught it red); fixed by distinguishing raw-vs-trimmed.
2. **The values.yaml bullets contradicted the new paragraph in the same block** (`""` no longer means cluster-wide under the default scope; `"*"` is no longer "equivalent to empty"). Rewritten to the new truth.
3. **The split-namespace caveat (docs-only per the ruling):** when `api.config.kubernetes.namespace` ≠ the release namespace, the derived single-namespace cache leaves the free-models refresher's catalog upsert unreachable. Documented in values.yaml, helm-values.md, and the runbook entry; the watch-both code change is flagged as an owner follow-up in the PR body, NOT built here.

## r2 — the guard mirrored the consumer; the workaround scope-qualified (bot review)

1. **The r1 guard leaked the same-class spellings:** `"*,"`/`"*,ns1"`/`"**"` (embedded `*` — parses to cluster-wide or a bogus `"*`"-named informer) and `"\n"`/NBSP (my `trimAll " \t"` is NOT Go's TrimSpace class) all sailed into the run-35872827066 death. The guard now mirrors the controller's own `parseWatchNamespaces` semantics: fail on `contains "*"` ANYWHERE plus a TrimSpace-equivalent whitespace-only test (`[[:space:]]` + U+0085 + Zs via regexMatch). Pinned by `TestRBACNamespaceScope_WatchAllGuardLeakShapes` (5 shapes); all green shapes stay green (full 8-test set + package). Template-literal lesson encoded in the fix: the regex backslashes must be `\\`-escaped (the template parser consumes `\x`/`\p` as its own escapes — the parse error named the line instantly).
2. **The split-ns workaround was a doc bug of my own making:** "list both namespaces" without scope qualification prescribes the SAME crashloop under namespace scope (the release-ns Role grants only leases/events/configmaps — a release-ns informer is Forbidden). All three sites now scope-qualify: both-namespaces is the remedy ONLY under cluster scope (or wherever RBAC covers both); under namespace scope, accept the refresher caveat.
3. **The refresher mischaracterization corrected (r2 minor finding):** not "per-tick NotFound noise" — the cached read for a namespace outside the cache set ERRORS (not IsNotFound), the upsert never runs, and the catalog NEVER exists (a per-tick "ConfigMap sync failed" error log; `refresher.go:234-236`, not 139-163). All three doc sites reworded.
4. The fail message's explicit-namespaces remedy now names the coverage rule ("namespaces your RBAC actually covers — under namespace scope that is the workspace namespace ONLY, NOT the release namespace").

## r3 — the comma-collapse branch, the stale in-file comment, Zl/Zp, NOTES.txt, the pure-defaults pin

1. **The comma-collapse guard hole (the blocker):** `","`/`" , "`/`",,,"` contain no `*` and are not whitespace-only — the r2 guard passed them verbatim into `parseWatchNamespaces`'s nil branch (`watch_namespaces.go:38-40`, proven by the repo's own `TestParseWatchNamespaces_AllEmptyEntriesReturnsNil`) → cluster-wide → the same death. The guard's empty-collapse test now includes COMMAS: the value fails the render when it is non-empty and matches `^[[:space:],\x{85}\p{Zs}\p{Zl}\p{Zp}]*$`. The comment's "mirrors parseWatchNamespaces" claim is now true of that branch too. Pinned: 3 comma shapes + U+2028/U+2029 (the Zl/Zp TrimSpace edge the r2 class missed).
2. **The stale in-file comment** (`controller-deployment.yaml` relay-URL rationale, "controller watches cluster-wide by default") corrected — the derivation's own file no longer contradicts it.
3. **NOTES.txt scope statement fixed:** "Role-scoped in {{ .Release.Namespace }} only" was wrong under the split-ns override (the workspace Role lives in the override namespace; the release ns holds leases/events/configmaps) — the operator-facing output now names the workspaceNamespace and the split-ns layout, the same family as the r2 docs pass.
4. **The refresher cite corrected:** 232-234 (the SyncConfigMap call + error log + return), not 234-236 (236 is the success log) — values.yaml fixed.
5. **The pure-defaults pin** (`TestRBACDefaultRender_DerivesWatchNamespaces`): no `rbac.scope` key at all — the `| default "namespace"` fallback must flow through the derivation; every prior pin set scope explicitly, so a silent values-default flip would have changed the default render uncaught.

## r4 — five vacuous pins (the r3 landing's own proof was hollow), the missed comment, the counts

1. **Five of thirteen guard subtests were VACUOUS — including every comma-collapse pin the r3 commit existed to land.** Mechanism (the bot's trace, reproduced): raw operator input in `t.Run` names — `value=,`, `value=*,`, … — lands in `t.TempDir()`'s path, and helm's `-f` is a pflag StringSlice that splits on the comma: writeFile succeeds at the full path, helm opens the comma-truncated first element, errors — `helmTemplateErr` returns that HARNESS error and `require.Error` passes with ZERO template evaluation. Mutation-proven by the bot (deleting the collapse branch left the r3 suite green). Fixed: subtest names are index-based (`case%02d`) — raw input never reaches `t.Run` names — AND every fail-shape pin now also asserts `err` contains `CrashLoopBackOff` (the guard's own message — a harness failure cannot satisfy it). Re-mutation-verified: under the well-formed collapse-branch mutation, CommaCollapse goes 6-red (5 subtests + parent) and LeakShapes 3-red (case03 newline + case04 NBSP + parent — the three star-bearing shapes correctly stay green under `contains "*"`). *(r5 correction: this entry first claimed "LeakShapes 6-red" — arithmetically impossible (5 subtests) and measured from a SLOPPY mutation whose unbalanced paren made every subtest fail on a template parse error; the failure messages I did not read said "unexpected right paren" outright. The mutation-verification itself was unverified — recorded from a broken experiment. The r4-added CrashLoopBackOff assertion is precisely what exposes that class: a parse error can never satisfy it.)*
2. **The second stale comment** (`values.yaml` inferenceRelay.workspaceRouterURL rationale, "the controller watches cluster-wide by default") corrected to the scope-conditional truth — the identical falsehood r3 fixed in controller-deployment.yaml, missed in the other file this PR edits.
3. **The counted-claims record corrected:** r3's "Pinned: 3 comma shapes" described three vacuous pins (they pinned nothing until this round); Files Modified now reads 10 pin functions (r3's "8" counted functions, not the added shapes). The suite's honest state at this head: 10 pin functions, every fail-shape now mutation-proven against its guard branch.

## r6 — the stale-posture family in the chart's own docs (three more survivors + a record fix)

1. **`helm/README.md:228`** (values table): "empty = all" → the derivation truth (empty derives the workspace namespace under the default namespace scope; empty = all only under cluster scope; any `*` fails the render under namespace scope).
2. **`helm/README.md:282-284`** (RBAC-scope section): the INVERSE claim ("cluster scope is required when watchNamespaces is empty") corrected — empty DERIVES under namespace scope; cluster-wide watching now requires explicit cluster scope. The redundant "combine with watchNamespaces=<release-ns>" advice replaced by the derivation statement + the crashloop cite. (r7 then tightened the "additional namespaces your RBAC covers" phrasing to the guard's own rule: under namespace scope the chart's RBAC covers the workspace namespace ONLY — additional namespaces need operator-created RoleBindings.)
3. **`docs/reference/cli.md:69`** (flag table): the identical `""`/`"*"` = cluster-wide falsehood — the r6 commit's first push said "not applicable" from a wrong-path grep (docs/cli.md instead of docs/reference/cli.md); the real site was found and fixed one amend later. The r6 commit message as first written therefore claimed "checked, not applicable" while the final diff APPLIES the fix — corrected in the message (r7).
4. **PROCESS (the r6 record):** this worklog entry itself was missing at r6 — my r6 notify said "worklog's r6 note rides in the commit," which was false (nothing rode; the entry did not exist). Recorded here per the counted-claims rule.

## r7 — the multi-tenant prescriptions (the family's most consequential survivors)

1. **`docs/operator/multi-tenant.md` "Combining with watchNamespaces" + "How to deploy per-tenant namespaces":** both prescribed `rbac.scope=namespace` + multi-tenant `watchNamespaces` with no RoleBinding qualification — that config RENDERS but CrashLoops at runtime (the chart's workspace Role binds only in the workspace namespace; the watched tenant namespaces' informers hit Forbidden LIST/WATCH — the run-35872827066 death class). Both sites now carry the load-bearing caveat: the chart creates NO per-tenant RoleBindings; bind the workspace Role in every listed namespace yourself.
2. **`docs/reference/helm-values.md` + `multi-tenant.md` scope tables:** "scoped to the release namespace" → the workspace-namespace geography (the `api.config.kubernetes.namespace` override); the storageclasses ClusterRole misattribution fixed (read-only, ALWAYS created regardless of scope — rbac.yaml:186).
3. The r6 commit message amended to the true cli.md story; the orchestrator's directive file (accidentally swept by an add-all) removed from the tree.

## r8 — the values.yaml rbac.scope block (the last survivor), two weak phrasings, the Files-Modified refresh

1. **`values.yaml:1194-1196` (the `rbac.scope` comment block):** storageclasses ClusterRole still misattributed to cluster scope there — contradicting the sibling doc corrected in r7. Fixed: the cluster bullet now says the read-only storageclasses ClusterRole is ALWAYS created regardless of scope; the cluster grant covers `llmsafespaces.dev/*`. *(r11 correction: this entry previously quoted the r8 diff as saying "…`llmsafespaces.dev/*` only" — the r8 text never contained that word; the quote interpolated the exact exclusivity formulation the r10 entry declares false.)*
2. **The same block's weak phrasings tightened** (the same family, minimal wording): the namespace bullet now states the WORKSPACE-namespace geography (the override), the derivation default, and the operator-RoleBindings rule for additional namespaces; the top-of-file namespace block's "controller can be configured to watch additional namespaces" now carries the same qualifier (under the default namespace scope, additional namespaces need operator-created RoleBindings — the chart creates none).
3. **Files Modified refreshed** to match the accumulated ten-file diff (it still described the r0 three-file PR).

## r9 — the template's own header (the family's last ship-in-every-render instance), the reads/writes trio, the test cite

1. **`rbac.yaml:22-25` (the Post-fix layout header, ships verbatim in every rendered manifest):** item 3 carried BOTH blocking falsehoods — storageclasses misattributed to the gated cluster grant (contradicting the file's own F1.3.7 note ten lines below) and the unqualified "may need to watch ALL namespaces when watchNamespaces is unset" (the pre-derivation inverse this PR exists to change). Fixed: watching ALL namespaces requires `rbac.scope=cluster` (empty DERIVES under the default namespace scope); storageclasses is the separate always-created reader (F1.3.7). *(r10 correction: this item also claimed "the gated grant is llmsafespaces.dev/* only" — FALSE, and contradicted by item 2 four lines below: the gated ClusterRole also grants cluster-wide READS on pods/secrets/PVCs/events/serviceaccounts/networkpolicies. The r10 header fix states the full grant.)*
2. **The "remain namespace-scoped regardless" trio** (values.yaml, multi-tenant.md, helm-values.md): under `scope=cluster` the gated ClusterRole grants cluster-wide READS (get/list/watch) on pods/secrets/PVCs/events/serviceaccounts + networkpolicies — only WRITES stay namespace-bound. All three sites now say writes-namespace-bound/reads-cluster-wide (the security-relevant direction: cluster scope's read blast radius must not be under-stated).
3. **The test cite corrected:** the no-mutating-verbs-under-cluster pin is `chart_test.go:1796-1826`, not :1411 (the PSA drop-ALL assertion).

Noted for the owner (the review's own suggestion, third round running): a docs-consistency repolint rule catching chart-behavior contradictions across `helm/` + `docs/` would close this recurring family mechanically.


## r10 — the fix's own sentence was the family's next member (the r10 review)

1. **rbac.yaml:27-30 — MY r9 replacement sentence was false:** "The gated cluster grant is scoped to `llmsafespaces.dev/*` only" — an exclusivity claim contradicted by the grant's own rules (pods/secrets/PVCs/events/serviceaccounts get/list/watch at :264-266, networkpolicies at :286-288), by the file's own :229-240 note, and by the SAME commit's trio edits. The first survivor that was not a missed straggler but language the fix commit itself wrote — in the security-relevant direction (an operator auditing cluster-scope blast radius from the shipped header would conclude the grant touches only custom resources, hiding cluster-wide reads of all secrets and pods). The header now states the full grant: llmsafespaces.dev/* full CRUD PLUS cluster-wide read-only on pods/secrets/PVCs/events/serviceaccounts/networkpolicies, writes namespace-bound. *(r11 correction: "the full grant" was false — the header omitted the gated ClusterRole's CONDITIONAL configmaps rule, active by default (read-only, via the free-models refresher) and full-CRUD cluster-wide under inferenceRelay; the r11 pass added it.)*
2. **rbac.yaml:38 (F1.3.3):** "never cluster" qualified — WRITES never; reads go cluster-wide under scope=cluster (the file's own carve-out).
3. **Same block, the cluster-grant intro** (:229-231): "needed when watchNamespaces is unset" was the unset-derives falsehood again — now states the derivation and the operator-RoleBindings rule (the block the r10 review anchored to; leaving it would have been r11's first finding).
4. **The r9 commit message contradicted itself between adjacent bullets** (bullet 1's "llmsafespaces.dev/* only" vs bullet 2's cluster-wide READS truth, four lines apart) — amended to the full-grant truth (the r6 message-vs-diff class). The worklog's r9 entry propagated the falsehood; corrected in place with the r10 marker above. Files Modified gains rbac.yaml (the r10 review's item 3).


## r11 — the configmaps omission (the ninth member), the lease misattribution, the "only" claims, the interpolation — and my own rules-first sweep

1. **rbac.yaml:27-32 omitted the gated ClusterRole's configmaps rule** — conditional (:279-281 relay → full CRUD cluster-wide; :290-292 freeModelsRefresher → read-only, ACTIVE UNDER DEFAULT VALUES) — while the r9/r10 records claimed "the FULL grant." One clause closes it; the parenthetical "writes stay namespace-bound" is now qualified (configmap writes go cluster-wide under relay+cluster — the population is exactly relay operators).
2. **rbac.yaml:16-19 (pre-existing): leases misattributed to the workspace Role** — the workspace Role grants no leases (they live in the release-ns Role, as item 2 itself said). Fixed.
3. **MY OWN SWEEP (the directive's instruction — enumerate from the RULES, not prose) found three further members the review did not list:** item 1's enumeration ALSO omitted the workspaces CRD and serviceaccounts; item 2 omitted its own conditional grants (agentd pin-cache configmaps; relay-credential secrets); the :246-249 cache-scope note enumerated only "pods + secrets + NetworkPolicies" (omitting PVCs/events/serviceaccounts/configmaps). All fixed against the actual rules block. *(r12 correction: FALSE — this sweep itself missed half of item 2's conditional grants: the release-ns Role has FOUR conditional branches (agentd-pins configmaps, opencode-pins configmaps — active in every real install since the delivery image is mandatory, relay configmaps+secrets full CRUD, refresher configmaps read+write), and the "fixed against the actual rules" claim was falsified by the very rules block it cited. The r12 strategy change retires the enumeration approach entirely.)*
4. **NOTES.txt + helm/README "only" claims qualified** — the always-created read-only storageclasses ClusterRole is cluster-wide even under namespace scope (NOTES now scopes the "only" to the WRITING Role; README names the storageclasses exception).
5. **Worklog :100 (the r8 entry) quoted a phrase the r8 diff never wrote** ("…only") — corrected in place with this marker; the r10 entry's "full grant" absolutism likewise corrected above. Lesson recorded: after two rounds of my own replacement prose becoming the family's next member, the only reliable enumeration source is the rules block itself — read before writing, every time.


## r12 — the strategy change: pointer-to-rules, invariants only (orchestrator ruling)

Twelve rounds demonstrated the structural failure: hand-enumerating grants in prose reliably omits members while claiming completeness (the r11 "rules-first sweep" itself missed half of item 2's four conditional branches — including both branches active in real installs, one with write verbs — while claiming it had enumerated "against the actual rules"). The durable shape, per the ruling: **prose that does not enumerate cannot omit.**

Every grant-listing site this PR touches now states only invariant-safe claims — (a) WRITES are namespace-bound under scope=namespace; (b) scope=cluster grants cluster-wide reads across the resource families its rules list; (c) conditional (delivery/relay/refresher) branches extend reads AND writes cluster-wide when active — followed by "the complete grant set is the rules block(s) in the chart's rbac.yaml, which are authoritative." No member lists, no "full grant" claims, no "only" claims a conditional grant can falsify. The run-35872827066 anchors and the operator-RoleBindings rule stay (invariant-safe).

Sites rewritten: the rbac.yaml header (items 1-3 + the cache-scope note), values.yaml's rbac.scope block, multi-tenant.md (scope table row + the cluster-mode paragraph), helm-values.md's rbac.scope row, NOTES.txt's namespace block, helm/README.md's RBAC-scope section. The commit message describes the doc change as pointer-to-rules, invariants only.

The recorded lesson: the rules block is machine-derived and authoritative; prose restatements of it are a drift hazard this PR has now demonstrated ten times — nine found by review, one (the interpolation) by record audit. Any future prose enumeration of these grants should fail review on sight.

## r13 (#1558, against 2d84a6e5) — the rebase invalidated three prose claims; the F1 premise; two pins

1. **Finding A (the r12 "invariants" were false against the grant's own base rule):** the gated ClusterRole grants full CRUD cluster-wide on the llmsafespaces.dev CR families UNCONDITIONALLY — my "cluster scope adds cluster-wide READS… conditional branches extend reads AND writes" frame under-stated write blast radius at FIVE sites (rbac.yaml header + cache-scope note, values.yaml block, multi-tenant.md table + paragraph, helm-values.md row) — the r10-class direction. All five now state the three-part truth: full CRUD cluster-wide on the CR families; cluster-wide reads on the CORE families (writes on those stay namespace-bound); conditional (RELAY/REFRESHER — "delivery" dropped, those live in the release-ns Role) branches extend both when active. "Even in cluster mode, WRITES stay namespace-bound" qualified everywhere to the core resources.
2. **Finding B (F1's premise false post-rebase):** #1552's relay-safe ClusterRole+Binding sits OUTSIDE the rbac.create gate — "create=false → the chart creates no RBAC at all / scope is grant-inert" was false in this tree. Both comment sites corrected to the precise contract: the chart creates none of the WORKSPACE-LIFECYCLE grants; coherence is the operator's out-of-band contract.
3. **Finding C:** NOTES.txt "the one base grant that is cluster-scoped" — post-rebase there are TWO always-created controller ClusterRoles (storageclass-reader + relay-safe CRD-watch). Fixed, pointer form.
4. **Finding D:** the "leases/events/configmaps" release-ns enumeration dropped at values.yaml + the runbook (flag-conditional drift; the crashloop conclusion stays); NOTES.txt "(the derived --watch-namespaces)" → "(the watch-namespaces arg — derived or explicit)".
5. **The two missing pins:** (i) TestRBACCreateFalse_UnsetStillDerives — the asymmetry's semantic content (derivation applies while the guard is create-gated) — MUTATION-VERIFIED red when the derivation is symmetrically create-gated. (ii) TestRBACDefaultRender_WatchAllFails — the pure-defaults guard shape. Mutation analysis recorded honestly: the review's hypothetical (keying the guard on exact scope-match, "re-opens the crashloop on the default-install population") is UNREACHABLE through helm — values.yaml itself defaults rbac.scope=namespace, so .Values.rbac.scope is never empty in a real render and both condition forms behave identically; the | default guards only programmatic template invocation. The pin therefore asserts real behavior (default install + "*" fails, message asserted) but cannot be mutation-reddened; recorded as defense-in-depth, not mutation-proven.

## r14 (#1558, against 0c9b6397) — this round's own four false statements, corrected from the rules

1. **The Finding-B parenthetical was INVERTED:** I wrote "the always-created storageclass/relay-safe ClusterRoles aside" — the rbac.create gate spans rbac.yaml:1→594 and ENCLOSES the storageclass-reader; the OUT-of-gate renders are the relay-safe CRD-watch pair (namespace scope only) and the llm-relay grants. Both comment sites + the NOTES block corrected to the actual conditions (relay-safe: namespace-scope-only, create-independent; storageclass: create-gated). NOTES.txt additionally claimed both exist "regardless of rbac.scope" — false for BOTH (relay-safe is namespace-only; storageclass is create-gated); now states each grant's own condition.
2. **The pure-defaults pin pinned the WRONG PATH and both its records were false in opposite directions:** the no-scope-key values exercised values.yaml's default (scope: "namespace"), not the template fallback — un-mutation-reddenable. The r13 worklog/commit claimed the fallback path "UNREACHABLE through helm" — also false (an explicit `rbac.scope: ""` override is a real render). The pin's values now pin the actual fallback (`scope: ""`) — MUTATION-VERIFIED red under the exact-match guard keying (the bot's empirical finding reproduced by me). Both records reconciled with this entry.
3. **The refresher attribution split at six sites:** "(relay/refresher) extend reads AND writes" was wrong for the refresher — the default-active branch grants configmap READS only at cluster scope (rbac.yaml:314-316; its writes ride the release-ns Role :190-195). All six sites now split: RELAY extends reads+writes; REFRESHER extends configmap reads only.
4. **Self-sweep per the directive (r11/r12 lesson), verified against the rules blocks before writing:** the create-gate boundary (:594), storageclass position (:214-243 inside), relay-safe scope condition (:610-612 "Rendered under NAMESPACE scope only"), refresher verbs (:314-316), relay configmap verbs (:299-313). Also fixed in the pass: multi-tenant's "Workspace CRs themselves" under-enumeration (→ the llmsafespaces.dev CR families incl. runtimeenvironments + inferencerelays-under-relay), the self-undermining core-resources parenthetical, and the pre-existing release-ns header enumeration (:22-24) gaining the refresher branch it omitted.
5. **PROCESS: `.scratch/820-closure-draft.md` was swept into r13's add-all (the r7 class, mine)** — untracked from the branch (kept locally for the closure standby per its directive; noted as a lesson: standby artifacts must be excluded before any add-all).
