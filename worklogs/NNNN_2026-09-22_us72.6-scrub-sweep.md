# Worklog: US-72.6 complementary close-out — nightly rogue-agent-sweep wiring + structural pins + #1537 verification

**Date:** 2026-09-22
**Session:** The FINAL story of epic-72 (relay-only key delivery) and the #820 close-out. The story's core (agentd `scrub-legacy-keys` + the boot-scheduled one-shot + the `LegacyKeysScrubbed` condition/event mirror + `local/us-72-rogue-agent-sweep.sh`) is the OWNER's in-flight #1537 (r1 review fixes at `ca60aeb0`, re-review pending). Per orchestrator ruling this session ships the #1536-pattern COMPLEMENT, never a replacement: the deliverables #1537 verifiably lacks — the nightly wiring of the sweep (post-drill, pre-dump) + the structural pin class for that wiring + the board row + the runbook evidence lane — plus an independent verification pass over #1537's r1 state with findings filed as a PR comment (the sanctioned channel).
**Status:** Complete

---

## Objective

Make US-72.6's exit criterion OBSERVABLE: the sweep (`agent_readable_files_keyfree`, design 0058 §8 line 201) must run on the nightly against the drill's flipped end state, with pins that hold the wiring's ordering, port isolation, and skip-guard semantics — so that the first post-merge nightly run produces the first EXECUTED evidence for #820 (no executed-evidence claims at merge time).

## Work Completed

### The nightly wiring (`.github/workflows/e2e-nightly.yml`)

- New step `Run the rogue-agent sweep (US-72.6, #820 exit criterion)` immediately after the drill, before the failure dump — it rides the drill's flipped-ON end state (relay on, namespaced scope, router up, mock-llm present), exactly the posture the sweep validates.
- Own `PORTFWD_PORT: 18089` (isolated from every other step's forward — the lib's `cleanup()` trap reaps its own forward on a normal exit, but a hard-killed script (runner cancellation SIGKILLs) skips the EXIT trap and leaks the bound port; a distinct port means a leaked drill forward can never break the sweep's `harness_start`), `CTX`/`NS`/`CLUSTER_NAME` per the established suite-step style. *(r1-corrected: the original "never reaps" claim was false — see r1; r2 further corrected the pin comment's set -e clause — errexit DOES run the EXIT trap, verified empirically.)*
- **SKIP-loud feature-detect** (the F8 valkey / #1342 creds precedent): the sweep script lands with #1537, NOT on main — until that merges, the step exits 0 naming the missing path and the pending vehicle so the nightly stays green while the row is honestly UNVERIFIED. After #1537 merges the same body executes the script with no further change.
- The step's comment block records the K1 non-frontable carve-out (owner-accepted): zero-raw-bytes is ASSERTED for frontable kinds only; non-frontable kinds (bedrock/vertex/azure_openai/opencode) are EXCLUDED pending the owner's K1 decision.

### The structural pin class (`local/us72_sweep_wiring_test.go`, the #1536 precedents)

- **Ordering pin:** us-70 rows < router build < drill pre-step < drill < sweep step < sweep invocation < failure dump — the sweep needs the drill's flipped state BEFORE it and must precede the dump (a sweep failure must be captured).
- **Port-isolation pin:** 18089 appears exactly once in the workflow; the step env carries `PORTFWD_PORT`/`CTX`/`NS`; the body manages no port-forwards itself.
- **Executable skip-guard pin (both branches, run under `bash -e`):** script ABSENT → exit 0 + LOUD skip naming #1537 + the missing path; script PRESENT (stub recording invocation) → the body EXECUTES it. The amputation/guard-inversion class (TestUS70AC1D) fails here, not in the first nightly run.
- **Guard/invocation path-identity pin:** the `test -f` guard and the `bash` invocation must reference the identical relative path — a typo in either half yields a step that always skips or always fails its first real run.
- Deliberately NO pins read the sweep script itself: it is #1537's file and absent from this branch — the wiring pins test the workflow only (zero file overlap with #1537).

### Board row + runbook evidence lane

- Board row US-72.6 → landed-state wording, vehicle-explicit and evidence-honest: #1537 (owner: scrub + one-shot + condition/event + sweep script) + this PR (nightly wiring + pins — the evidence lane); first EXECUTED evidence begins with the first post-merge nightly run; #820 closes on that green run (Refs until then). Merge-order note: if this merges before #1537 the row's #1537 reference is forward-looking and the sweep step SKIPs loudly until it lands. *(r1-corrected below: the closure narrative and the "landed" wording were both inaccurate — see the r1 entry.)*
- `docs/runbooks/relay-only-flip.md` additive section "Post-flip evidence: the rogue-agent sweep (US-72.6, #820)": what R1/R2 prove, the skip semantics (a SKIP row means UNVERIFIED, not passed), the K1 carve-out, and the closure rule (closes on the first recorded green run, never on a merge).

### The #1537 verification pass (findings filed as a PR comment, nothing pushed)

Independently verified on the `pr-1537` ref (read-only): `go build ./...` clean; the scrub/tracker/hook suites (`./cmd/workspace-agentd/ -run 'Scrub|LegacyScrub|RelayLiveness|RelayEmission'`), the controller mirror suite, and all 6 `TestUS72Sweep_*` pins pass locally — the r1 disposition's claims reproduce. Findings for the owner's next round (all filed on #1537, none blocking my wiring):

1. **R2's `LegacyKeysScrubbed=True` assertion is decorative** — the manual in-pod `scrub-legacy-keys` exec is a separate process whose report never reaches the running agentd's tracker; the True observed is the BOOT scrub's `Clean` verdict (the plant happens after boot), so the row passes regardless of R2's scrub. The scheduled-path e2e needs a Surface-2 plant (an `agent-config.json` copy under `.local` — the one surface init-fs does NOT manage) followed by a pod cycle, asserting reason `KeysRemoved`.
2. **The sweep's fixed 6-path list cannot validate Surface 2 or the design's "every uid-1000-readable path"** — post-scrub zero passes even if a scrubbed copy still carries the canary (the list never looks under `.local/config*`); missing surfaces include the PVC-persisted XDG registry copy (`/home/sandbox/.config/opencode/opencode.json`) and user-writable PVC paths. Recommend a find-based sweep over the PVC mounts + XDG dir + a name-based rootfs catch.
3. Unaddressed review round-1 minors/robustness: the error-path event still says "complete" omitting the error (finding 4); `AuthPathSkipped` dropped in the tracker→CRD conversion (finding 5); `sync.Once` guards the attempt not the outcome (no retry); sidecar mode never scrubs (`--sidecar` dispatch precedes the hook wiring); `stripKeysFromFile` rewrites non-atomically (no temp+rename); stale "statusz" comments (Rule 4); no ExecuteSmoke for the script itself (the harness rule — the owner's to fix; my wiring carries its own executable pin).
4. Offered to take the #820 comment-5769972896 items (the three unwired envtest suites + the per-function repolint guard) in THIS PR if the owner wants — otherwise they stay owner-decision per the filed disposition.

## Key Decisions

1. **Complement, never replace (orchestrator ruling):** #1537 is the owner's actively-iterated vehicle (they pushed the r1 fixes themselves ~50m after my spawn). Zero pushes to their branch; findings via PR comment; my PR touches none of #1537's files.
2. **SKIP-loud feature-detect over hard-fail:** wiring a script that exists only on an unmerged branch would break the nightly at merge-order risk; the F8/#1342 precedent (SKIP loudly naming the dependency) keeps the nightly green while honestly marking the row UNVERIFIED — and needs zero changes when #1537 lands.
3. **Pins on the workflow only:** reading the sweep script from my tests would couple this PR to #1537's tree (and fail on main where the file is absent). The wiring pins prove ordering, isolation, and both guard branches — everything I own.
4. **The envtest items stay owner-decision:** the #820 comment's disposition says "owner decision requested"; the bot's #1537 closure check wants them before epic close — I offer to take them, I do not fold them unilaterally.

## Blockers

None. **#820 actual state (r1):** the issue is ALREADY CLOSED — auto-closed at #1534's merge (2026-09-22T09:04:19Z, its body said "closes #820"), ~4h before this PR existed and with ZERO sweep runs executed. The design's exit criterion (a recorded green sweep run) is therefore UNMET while the issue reads COMPLETED; reopening is an owner decision (in flight — do not reopen unilaterally). This PR's artifacts record the discrepancy factually (runbook, board row) so the record is honest wherever the reopen lands.

## Tests Run

- `go test ./local/ -run 'TestUS72SweepWiring' -count=1 -v` — 4/4 green (grown red-first: all four failed before the workflow step landed).
- `go test ./local/ -count=1` — full package green (the #1536 reconciliation pins + the owner's drill pins unaffected by the insertion).
- `go run` sigs.k8s.io/yaml parse of `.github/workflows/e2e-nightly.yml` — clean.
- Verification of #1537 on its ref (read-only): `go build ./...`; `go test ./cmd/workspace-agentd/ -run 'Scrub|LegacyScrub|RelayLiveness|RelayEmission'`; `go test ./controller/internal/workspace/ -run 'LegacyScrub|CheckAgentHealth'`; `go test ./local/ -run TestUS72Sweep` — all green (no code of mine).
- `make repolint` — all checks passed; `golangci-lint run ./local/...` — 0 issues.

## Next Steps

1. #1537 merges → the sweep step goes live on the next nightly automatically (no wiring change needed).
2. First green recorded sweep run → the design's exit criterion is satisfied — the evidence the owner needs for the #820 reopen-or-leave decision (the issue is already auto-closed; see Blockers).
3. Owner decisions parked on #820: the reopen call (the exit criterion was unmet at auto-close); the K1 non-frontable carve-out resolution; the three unwired envtest suites + per-function repolint guard (comment 5769972896 — offered to this session if the owner wants); the KMS `PubUnreadable` wiring deferral record.

## r1 — three documentation-accuracy blockers (bot review 13:42Z), all verified true before fixing

1. **The closure narrative was FALSE.** The runbook, board row, and worklog claimed "#820 closes on the sweep's first green nightly run — never on a merge". Reality: #820 was AUTO-CLOSED at #1534's merge (stateReason COMPLETED, closedAt 2026-09-22T09:04:19Z — verified via the issue API; #1534's body carries "closes #820") four hours before this PR existed, with zero sweep runs. I asserted a closure RULE that the platform had already falsified — the exact premature-closure class the repo keeps catching, shipped by me in the artifacts whose entire job was evidence honesty. All three artifacts now state the actual state: auto-closed before the sweep ever executed; exit criterion unmet; reopen is an owner decision. Not reopening unilaterally.
2. **The board row overstated.** "landed across two vehicles" — #1537 is OPEN (verified unmerged at fix time). Re-worded "landing across two vehicles — #1537 (owner, OPEN at this writing …)" (the merge-order hedge committed IN the row, not just the PR body).
3. **The port rationale was FALSE.** My pin comment and worklog claimed the drill's forward "is never reaped" — false: `local/lib/us70-common.sh:83-93` has `cleanup()` killing/waiting `PF_PID` via an EXIT trap, and the drill sources it. The honest rationale for the distinct 18089: defense against a hard-killed script (runner cancellation SIGKILLs; the EXIT trap never fires) leaking a bound port. Re-rationaled in both places.
4. Style/nits also landed: `!strings.Contains(...)` (was `== false`); the K1 wording unified across workflow comment + runbook + worklog ("ASSERTED for frontable kinds; non-frontables EXCLUDED pending the owner's K1 decision" — one phrasing everywhere); pin (b) now also asserts `CLUSTER_NAME` in the step env. The skip-guard stub env-carry nit (string-checked only) left as-is — non-blocking, and the executable pin's marker check carries the weight.

The lesson of r1, stated plainly: I verified every claim against CODE and TESTS but never against the ISSUE TRACKER — the one artifact my PR was nominally about. Evidence-honesty claims must be checked against the live state of the thing they describe, not just against the diff.

## Files Modified

`.github/workflows/e2e-nightly.yml` (the sweep step), `local/us72_sweep_wiring_test.go` (new, 4 pins), `design/stories/epic-72-relay-only-key-delivery/README.md` (board row), `docs/runbooks/relay-only-flip.md` (additive evidence-lane section), this worklog.
