# Worklog: US-72.6 — legacy-key scrub + rogue-agent sweep (closes #820)

**Date:** 2026-09-22
**Session:** Epic 72's final story (design 0058 §8): remove legacy plaintext from PVCs and prove the epic's exit criterion end-to-end — zero provider-key bytes in the pod (K1).
**Status:** Complete

---

## Objective

The agentd `scrub-legacy-keys` subcommand (narrow: platform-shaped paths only), the boot-time trigger on post-flip pods with the healthz (/v1/healthz) → controller condition/event mirror, and the rogue-agent e2e sweep with a positive control + the residue-boot row — #820's close-out.

## Work Completed

- **The scrub** (`cmd/workspace-agentd/scrub_legacy_keys.go`): exactly two surfaces under the workspace root — the pre-US-35.7 REGULAR-FILE auth.json at `.local/opencode/auth.json` (key fields stripped, structure preserved) and `agent-config.json` copies under `.local` (provider options.apiKey stripped, bounded walk). NEVER touches: the live symlink (the #1296 shape — pinned), user files (byte-identical survival pinned, even with key-shaped fields), anything outside the shapes. Idempotent (AlreadyClean, pinned). Malformed legacy files surface loudly; malformed COPIES never abort the walk.
- **The trigger + mirror**: the relay monitor's FIRST Present=true observation (a post-flip pod) fires the one-time scrub (sync.Once); the static report rides healthz/statusz (`LegacyScrubHealth`); the controller's `mirrorLegacyScrub` sets the new `LegacyKeysScrubbed` condition (True/KeysRemoved, True/Clean, False/ScrubError) and emits the `LegacyKeysScrubbed` event EXACTLY ONCE per outcome — the persisted `llmsafespaces.dev/legacy-scrub-reported` annotation makes the idempotency survive controller restarts. 4 mirror pins: removals+exactly-once (eventsFrom drains — zero NEW events on re-observation is the proof), clean-is-quiet, error surfaces, no-slice-is-silent.
- **The sweep** (`local/us-72-rogue-agent-sweep.sh`): R1 the exit criterion (zero canary bytes across the §1.2 inventory + EVERY `/proc/[0-9]*/environ`); R2 the positive control (plant the pre-US-35.7 regular-file residue → the sweep FINDS it → exec the scrub subcommand in-pod → post-sweep zero + LegacyKeysScrubbed=True observed on the Workspace). 5 shape pins (rows-in-order with the can-fail assertion, the surfaces + scrub-exec markers, the counting branch byte-matches the drill's drilled form, isolation). First cluster execution rides the reviewer runner / #1456 lane (the standing disposition, stated up front).

## Key Decisions

1. **Strip, don't delete**: the scrub removes KEY MATERIAL, preserving file structure and sibling fields — user-visible damage surface is minimal and idempotency is structural.
2. **The trigger is the batch's relay presence** (the same signal the US-72.4 monitor reads): flag-off pods never scrub (their raw keys are the working path); the sync.Once bounds it to once per pod lifetime.
3. **The event idempotency is annotation-backed** (not condition-dedupe): conditions are idempotent by setCondition, but the EVENT would re-fire every reconcile against the static report — the `legacy-scrub-reported` annotation stamps the last-reported outcome; same-outcome re-observations are silent, an outcome CHANGE (impossible today, possible if the scrub ever re-runs after a PVC restore) re-fires honestly.

## Assumptions → validation record

- "The auth path under a post-#1296 pod is a symlink" → pinned (SymlinkAuthUntouched; the resolved store is not the scrub's path).
- "User files with key-shaped fields exist in real trees" → the UserFilesUntouched fixture uses exactly that shape; byte-identical survival asserted.
- "The controller observes LegacyScrub via the existing statusz poll" → the mirror rides checkAgentHealth's healthResp (the Relay/SpawnEnv precedent); 4 pins green.

## Blockers

None. The sweep's first RECORDED execution rides the reviewer/#1456 (stated in the PR body).

## Next Steps

1. #820 closes on the sweep's first green cluster run (the R1 zero-canary row) — the disposition is on the PR.
2. Post-close: the follow-up ledger carries the chart-pins-skip-silently-without-helm note (the orchestrator's r6 observation on #1534) and the pre-commit worklog self-numbering hook fix.

## Tests Run

- `go test ./cmd/workspace-agentd/` — ok (303s, incl. the 6-test scrub matrix).
- `go test ./controller/internal/workspace/` — ok (68s, incl. the 4 mirror pins).
- `go test ./pkg/agentd/ ./local/` — ok (5 sweep pins).
- `go build ./...` — clean.

## Files Modified

- `cmd/workspace-agentd/scrub_legacy_keys.go` (+ 6-test matrix), `legacy_scrub_tracker.go`, `legacy_scrub_snapshot_helper.go`, `main.go` (dispatch + wiring), `server.go`/`healthz.go` (snapshot threading), `relay_liveness.go` (first-Present hook + scrubOnce)
- `pkg/agentd/types.go` (LegacyScrubHealth + the response field)
- `pkg/apis/llmsafespaces/v1/workspace_types.go` (WorkspaceConditionLegacyKeysScrubbed)
- `controller/internal/workspace/health.go` (mirrorLegacyScrub) + `legacy_scrub_mirror_test.go` (4 pins)
- `local/us-72-rogue-agent-sweep.sh` + `local/us72_sweep_test.go` (5 pins)

## r1 — three real bugs, all mine, all caught by review

- **The depth bound was dead code**: `filepath.SplitList` splits PATH LISTS on ':', never bounding anything — the walk recursed the whole .local subtree (the user's entire toolchain: MISE_DATA_DIR, GOPATH, CARGO_HOME...) and would scrub user-authored agent-config.json copies at any depth. Fixed with strings.Count on the separator; pinned by TestScrubLegacyKeys_DepthBoundStopsAtKnownShapes (a 4-deep user copy survives byte-identical while the known shape scrubs).
- **The event idempotency never persisted**: the annotation stamp rode ws.Annotations, but the health-check persists via Status().Update which DROPS metadata — the event would re-fire every ~15s reconcile forever. Fixed with the force_recycle precedent: fresh Get → mutate annotations → full Update → RV-sync (updating the live object directly would clobber its accumulated status mutations — the fake client resets status on plain Update under a registered subresource; the first attempt panicked on a nil map into the bargain). Pinned by TestCheckAgentHealth_LegacyScrubRoundTrip: the PRODUCTION observation pattern (fresh Get per reconcile) — the persisted annotation, not a shared in-memory pointer, carries the idempotency.
- **The sweep's R2 was structurally broken**: init-fs installs the #1296 SYMLINK at the auth path on every modern pod; a bare `>` FOLLOWS it into the live rt/auth.json store (a surface the scrub deliberately never touches) → post-scrub hits ≥1 → guaranteed die. Fixed: rm -f the link, then plant the true regular-file legacy shape; pinned by TestUS72Sweep_PlantBreaksTheSymlink (order-asserted).
- Missing tests landed: the depth pin, the round-trip pin, the tracker units (nil-before-run, exactly-once, error-recorded), the monitor hook unit (exactly-once across evaluations; never on absent), the malformed-shape pair (loud legacy, walk-continuing copy).
- `Closes #820` → `Refs #820` in the PR body until the sweep's first recorded green run.

## r2 — the one-level hole and the honest evidentiary rows

- **Depth off-by-one (r2 finding 1)**: the bound lived only on the DIRECTORY branch — a file directly at 3 separators (its parent has 2) was still scrubbed. Fixed with the file-level bound; the pin now carries BOTH boundary depths (3 = the hole, 4 = the SkipDir case), red-first verified.
- **Error event text (finding 2)**: the error path now emits Warning "one-time legacy-key scrub ERRORED: <err>" — pinned by text assertions (names the error, never claims completion).
- **R2's evidentiary honesty (finding 3)**: the row now CAPTURES the scrub subcommand's JSON report and asserts authKeysRemoved >= 1 from it — the direct evidence; the condition row is re-attributed to the BOOT mirror (a clean pod reports AlreadyClean; the subcommand's scrub is a separate process the spent sync.Once cannot mirror) with the comment saying exactly that.
- **Closing keywords (the disposition blocker)**: title and the body H1 switched to "refs #820" — the squash-merge title was a SECOND close path.
- **The statusz comment fiction (Rule 4)**: all five sites corrected — LegacyScrub lives on HEALTHZ (/v1/healthz, the surface the controller polls); StatuszResponse has no such field.

## r3 — the path-to-approval items

- **The four surviving statusz misstatements + the PR body line + this worklog's Objective**: corrected (grep-verified `grep -rn "statusz" cmd/workspace-agentd/scrub_legacy_keys.go cmd/workspace-agentd/main.go cmd/workspace-agentd/server.go` returns only correct-context hits; every mention now says healthz /v1/healthz, the surface the controller polls).
- **The healthz render pin**: TestHealthz_RendersLegacyScrubSlice — a non-nil snapshot RENDERS legacyScrub in /v1/healthz's JSON (auth/config counts asserted); nil omits the field.
- **The tracker concurrency claim is now exercised**: 8 concurrent runOnce callers under -race; exactly one scrub (the comment's claim, actually tested).
- **R3 — the residue-boot migration row**: plant on the PVC while running → suspend → resume → the BOOT scrub fires (LegacyKeysScrubbed=True/KeysRemoved auth=1) + post-boot sweep zero. The actual migration scenario through the real trigger — the reviewer's finding that even a green first run would never have exercised it. Pins updated (three-rows-in-order + the new markers).
- Item 4's alternative branch taken: the R3 row added; the first recorded cluster run requested from the reviewer's runner (the standing disposition) — that run now exercises the central trigger end-to-end.

## r4 — R3's two structural bugs (both the plant-vs-platform class)

- **The Surface-1 plant never survived to the scrub**: init-fs MANAGES the auth.json path — replaceSymlink deletes any pre-existing regular file before installing the #1296 symlink (reviewer-verified empirically with a verbatim replication). The fresh pod booted residue-free; the scrub saw a symlink → AlreadyClean → the auth=1 assertion was structurally unpassable — the r0-R2 bug class reintroduced one row later, exactly as the reviewer said. Fixed: R3 plants SURFACE 2 (the agent-config.json COPY under .local/config/opencode/ with a raw apiKey — scrub territory init-fs never touches); NEW PIN TestUS72Sweep_R3PlantIsInitFsSurvivable cross-checks the plant path against init_fs.go's managed-path source (the survivability contract, structural).
- **The poll tripped on R2's stale condition**: nothing ever removes LegacyKeysScrubbed, and R2's boot-mirror already left True/Clean "clean" pre-suspend — a non-None poll broke on iteration 1 with the stale message. Fixed: poll for reason=KeysRemoved + config=1 in the message.
- Worklog residuals swept (the r0-section statusz mentions corrected in place; counts now 7 sweep pins). The r0 COMMIT SUBJECT's "(closes #820)" stands as pushed history — noted here rather than rewritten; the title + body carry refs, and the merge title comes from the PR title (refs).
