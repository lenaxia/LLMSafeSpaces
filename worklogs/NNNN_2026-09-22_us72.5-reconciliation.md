# Worklog: US-72.5 close-out — reconciliation after the owner's #1534 flip

**Date:** 2026-09-22
**Session:** US-72.5 landed in three vehicles: the unit-0 envtest-matrix repair (#1531, merged aac517ad), the owner's #1534 (default flip + runbook + `local/us-72-relay-only-flip-drill.sh` + render pins, merged 924493c9), and this reconciliation PR (#1536) — reduced from my original story PR after #1534 superseded its flip/drill/runbook core. This PR carries exactly what main lacked post-#1534: the cluster-scope consumer relay-OFF pins (without which the next nightly/pool/attachments/bootstrap/s5/us2 installs FAIL their render — the chart refuses relay-on + cluster scope), the nightly wiring of the OWNER's drill script (untouched — the executed-rollback evidence mechanism), additive runbook deltas, and the board row.
**Status:** PR #1536 (reduced in place)

---

## Objective

Make main's flipped default OPERATIONALLY TRUE: every cluster-scope harness/consumer install that the flip renders-refused pins relay off (the landed runbook's own documented remedy); the owner's drill runs on the nightly (executed-rollback evidence begins with the first post-merge run); the runbook gains the accuracy deltas the flip makes load-bearing; the board row closes.

## Work Completed

- **Consumer relay-OFF pins ×7** (the urgent half — each install was render-refused on its next run): `values-cluster.yaml` (this deployment's values: cluster scope, explicit off + runbook pointer — migrating the deployment to relay-only is an operator flip per the runbook), `.github/workflows/e2e-nightly.yml` + `us-70-delivery-pool.yml` + `e2e-attachments-single-container.yml` install steps, `local/bootstrap.sh`, `local/s5-overlay-validation.sh`, `scripts/us2-kind-integration.sh` — `relayOnlyKeyDelivery.enabled=false` pinned next to each `rbac.scope=cluster` (no mid-continuation comments — the amputation bug class the nightly itself documents).
- **Nightly wiring of the owner's drill (script untouched):** router image build + `kind load` step (the BYO router defaults to the unreachable-on-kind ghcr.io registry); a pre-step that puts the release into the valid drill shape (`relayOnlyKeyDelivery.enabled=true` + `rbac.scope=namespace` + the local router image, `--wait`) — necessary because the drill's own `flip()` sets only the relay flag and the nightly's install is cluster-scope (render-refused); under `--reuse-values` the script's R1 `flip true` is then an idempotent re-set, R3/R4 cycle within namespaced scope, and the drill ends flipped (the runbook's target posture; kind teardown follows); the drill step runs AFTER the us-70 rows its `mock-llm` Service dependency creates.
- **Additive runbook deltas** (the owner's landed prose untouched): the fast rollback lever (`relayOnlyKeyDelivery.api.enabled=false` — builder-only, staging/router stay up, chart-pinned); the namespace-termination budget on full rollback (630s router grace cap; helm `--wait` does not cover ns deletion; no re-flip while Terminating; re-flip = fresh keypair → force-re-seal); a "Known behaviors (flip-state accuracy)" section — the K1 non-frontable carve-out (bedrock/vertex/azure_openai/opencode stay raw under flag-on, audited via `relay_raw_emission` — the landed unqualified K1 claim overstates), relay-condition freeze on flag-off, the `PubUnreadable`/KMS-wiring deferral (design 0058 trade-off (iii), owner decision pending), the no-live-latency-histogram note (counters only; the p99 evidence is the #1432 e2e budget), and relay-liveness probe quota sizing.
- **Board row** US-72.5 → landed (#1531 + #1534 + #1536), with the evidence state explicit: canary + rollback EXECUTED evidence begins with the first post-merge nightly drill run — the row does not claim it.
- **Structural/executable pins** (`local/us72_reconciliation_pins_test.go`, the TestUS70AC1D precedents): the pre-step body EXECUTED against fake helm/kubectl (argv asserted for the four drill-shape sets + both rollout waits — the mid-continuation amputation class); nightly ordering (us-70 mock-llm rows and the router-image build BEFORE the drill, drill before the failure dump); the router-image build/load step; and the hard regression gate — a table over ALL SEVEN cluster-scope consumer sites asserting each cluster-scope install carries its relay-OFF pin (occurrence-count equality per site; a silently dropped pin reintroduces the identical next-run install failure undetected).
- **Dropped from the pre-#1534 PR** (superseded or contra-owner, per the orchestrator's rulings): my drill script + its 9 structural pins, my runbook body, the Epic-42 exact-name finder fixes (the owner solved the collisions via relay-off pins inside the fleet-test values), and the G16 ingress master-toggle template fix + pin (the owner DELIBERATELY documented the ingress policy as flag-gated, not toggle-gated, in the landed runbook's "Known interactions" — their call, landed).
- **Shelved, never pushed:** the prepared unit-C flip branch (`feat/epic-72-us72.5-default-flip` @d86c7098) — mooted by #1534; its consumer OFF-pins are re-landed here against main.

## Key Decisions

- **In-place reduction of #1536** (rebase onto 12d46502 + force-with-lease, the established correction precedent) rather than close-and-reopen: keeps the review thread; the diff vs main is exactly the remainder.
- **Pre-step wiring design** over touching the owner's script: their shape stands; the workflow moves the release into the script's valid operating envelope. The nightly install stays raw-shaped for all other rows (minimal blast radius); the drill's flipped end-state is disposed by the kind teardown.
- **G16 exception respected, not relitigated:** the landed runbook documents the ingress policy's flag-only gating with the operator remedy (pin relay off to omit ALL policies); the adjusted G16 test encodes it. Reopening that contract is an owner decision, recorded here, not acted on.

## Blockers

None. **Time-sensitive:** the next nightly run dies at its install step until the OFF-pins land — this PR is the fix.

## Tests Run

- `go test -count=1 ./helm/` on the reduced branch — ok (chart + chart tests untouched by this PR; main's suite green).
- `go test -count=1 ./local/` — ok (the owner's `us72_flip_drill_test.go` untouched; my pins file dropped).
- `bash -n` on `local/us-72-relay-only-flip-drill.sh` (owner's, unmodified), `local/bootstrap.sh`, `local/s5-overlay-validation.sh`, `scripts/us2-kind-integration.sh` — clean.
- YAML parse of all three touched workflows + envtest.yml (regression) — clean.
- `make repolint` — all checks passed; `golangci-lint` on touched packages — 0 issues (no Go changes in this PR).

## Next Steps

- First post-merge nightly: the drill's first EXECUTED run (executed-rollback evidence begins there). Watch-item (owner-side remedy if it flakes): the drill's tight R3→R4 cycle may race llm-relay namespace termination (router 630s grace; helm `--wait` does not cover ns deletion).
- US-72.6: PVC scrub + the rogue-agent sweep (#820 close-out) — the owner's drill `sweep_hits` is the reference sweep; scope it to frontable kinds per the K1 carve-out above.
- Owner-decision items parked on #820: the KMS `PubUnreadable` split (design 0058 (iii)); the three unwired envtest suites + guard granularity (comment 5769972896); optionally the G16 master-toggle contract vs the documented exception; optionally a router latency histogram.

## Files Modified

`values-cluster.yaml`, `.github/workflows/{e2e-nightly.yml,us-70-delivery-pool.yml,e2e-attachments-single-container.yml}`, `local/{bootstrap.sh,s5-overlay-validation.sh}`, `scripts/us2-kind-integration.sh`, `docs/runbooks/relay-only-flip.md` (additive), `design/stories/epic-72-relay-only-key-delivery/README.md` (board row), `COORDINATE.md`, this worklog.
