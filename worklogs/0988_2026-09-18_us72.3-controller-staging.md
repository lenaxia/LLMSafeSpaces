# Worklog: US-72.3 — Controller staging + Workspace conditions + policy flag

**Date:** 2026-09-18
**Session:** Implement epic-72 US-72.3 (design 0058 §4.1 hop 1–2, §4.2 controller legs, §4.6, §4.9 lifecycle): the controller seals bound BYO llm-provider credentials into `llm-relay` envelope Secrets, mints scoped tokens from the router's internal mint endpoint, stages them in a workspace-namespace handoff Secret for the (US-72.4) batch builder, sets the three new conditions, owns the DR/residual-window terminator and seal-time pub-generation validation, and wires the deployment flag + fail-loud startup guard.
**Status:** Complete

---

## Objective

Land every US-72.3 AC: staging reconcile step; `CredentialsStaged`/`CredentialStale`/`CredentialRejected` conditions with §4.6 cause enumeration (incl. corruption/wrong-pub); revocation = envelope Secret deletion with same-pass `UnregisterStagedKey`; the §4.2 reconcile predicate (re-get pub → compare generation → re-seal; corruption-class stale + intact spawn-layer lineage → anti-storm-bounded rotate escalation; suppressed for revocation/delivery/token-expiry classes); seal-time generation validation against the rotate response; `relayOnlyKeyDelivery.enabled` controller flag (default false, zero behavior change off) with a fail-loud startup guard; the named test plan (`dr_window_reconcile_terminates`, `pub_sealtime_generation_validated`, stage/unbind/rotate reconcile legs, `mixed_fleet_batches`, envtest conditions matrix, helm-render tests).

---

## Design decisions (settled this session)

### D1 — Credential source seam: a minimal API internal endpoint (unavoidable, implemented)

The controller must seal **plaintext** provider keys, but decrypted BYO credentials
exist only transiently inside the API (Postgres rows are ciphertext; K8s holds none of
them). No existing surface serves the controller:

- `pod-bootstrap` returns decrypted data but is authenticated by the **pod's** projected
  SA token (TokenReview) — the controller holds no such token, and the response shape is
  batch entries, not the provider list.
- `GET /api/v1/internal/orgs/:id/status` is org metadata only.

Settled seam (the OrgStatusClient precedent, verbatim pattern):

- **API side (new, small):** `GET /api/v1/internal/workspaces/:workspaceID/llm-providers?ownerUserID=<uid>`
  — constant-time `X-Internal-Token` (`LLMSAFESPACES_INTERNAL_TOKEN`, fail-closed 403
  when unset, 401 on mismatch — identical to the org-status handler), backed by a new
  `SecretService.ResolveLLMProviders(ctx, ownerUserID, workspaceID)` that reuses
  `loadWorkspaceRows` + `decryptBindingWithDEK` + `applyModelAllowlist` with the same
  dedup-by-slug-first-winner semantics as the batch builder, so the staged set is by
  construction exactly the batch's provider set. Every reveal is audited
  (`internal_llm_providers_reveal`).
- **Controller side:** `CachedLLMProviderSource` (60s TTL, success-only cache — a fetch
  failure fails the staging pass loudly; no stale-serve, unlike org status: credentials
  must be fresh and a stale set could seal revoked material).

K6 is unaffected: this is controller→API trusted-plane, never a pod delivery path.
Documented as required by the story ("if a small API-side internal endpoint is
unavoidable, implement it and document why").

### D2 — Token handoff Secret (shape owned here, consumed by US-72.4)

- Name: `workspace-relay-<workspaceName>` in the **workspace namespace**, owner-ref'd to
  the Workspace (GC on terminate, survives suspend).
- Single data key `handoff`, one JSON document:

```json
{
  "revision": "<staged revision>",
  "routerURL": "http://llm-relay-router.llm-relay.svc.cluster.local",
  "providers": [
    {
      "providerSlug": "openai",
      "kind": "openai",
      "token": "lrt_...",
      "routerPath": "/w/<workspaceName>/<slug>/v1",
      "baseURL": "https://api.openai.com/v1",
      "modelAllowlist": ["gpt-4o"],
      "keyID": "hpke-g2",
      "expiresAt": "2026-09-19T07:00:00Z"
    }
  ]
}
```

- **Staged revision** = deterministic content hash (`sha256` over sorted
  `slug|envelope|token-expiry` per provider, hex[:12], `r`-prefixed) — changes exactly
  when any envelope or token changes, so US-72.4 can compare it against `spawned_rev`,
  and `CredentialsStaged` carries it in its message.
- Renewal: a token is re-minted when it is past TTL/2 (design §4.4); the mint check is
  in-pass on every Active reconcile (requeue cadence 15s), zero API calls at steady state.

### D3 — Envelope Secret contract (matches the router's informer cache exactly)

`byo_cache.go` already fixes the read-side contract; this story writes it:
name `llm-relay-env-<workspaceName>-<slug>` (slug is already `^[a-z0-9-]+$` by the DB
CHECK), labels `llmsafespaces.dev/workspace-id: <workspaceName>` +
`llmsafespaces.dev/provider-slug: <slug>`, data `envelope` (the `stg:v1:…` wire string)
+ `models` (JSON array of allowlisted model IDs — the router's zero-upstream `/models`
catalog). The mint-key Secret (`llm-relay-mint-key`, data key `mint-key`) is created by
the controller (adopt-if-exists) — `byo_run.go` documents that expectation.

### D4 — Staging mode: HPKE now, KMS loud-not-yet

The controller resolves its sealer from `llm-relay-hpke-pub` (name-scoped `get`, with
`llm-relay-mint-key` — see the §4.3 amendment). A pub Secret that is absent OR
shape-unreadable fails the pass loudly as `CredentialsStaged=False`/`PubUnreadable`,
with a message naming both possibilities: an unbootstrapped router, or a KMS-mode
deployment (`llm-relay-kek`) where controller staging is not yet wired. The controller
deliberately holds NO `get` on `llm-relay-kek`, so it cannot distinguish the two (design
§4.3 amendment trade-off iii; the r3 review removed the probe as RBAC-dead code) — KMS
staging mode additionally requires RootKeyProvider construction in the controller
(cloud credential plumbing that does not exist controller-side today; neither US-72.2's
router wires it — the fleet runs HPKE-only). Landing KMS construction is flip-prep work
(US-72.5); the seam (`StagingSealer` interface) means no structural change later.

### D5 — Kind coverage: stage only HTTP-frontable kinds

The BYO router is a Bearer-injecting reverse proxy to exactly one upstream. That covers
the plain-REST kinds. `pkg/secrets.ProviderDefaultBaseURL(kind)` (new, single table next
to `LLMProviderData` so US-72.4's builder swap reuses it) resolves first-party defaults:
openai, anthropic, google, cohere, mistral, perplexity, groq, xai, openrouter, together;
`openai_compatible` requires an explicit BaseURL. NOT staged (event + named in the
`CredentialsStaged` message, raw-key path continues — mixed-fleet semantics):

- `bedrock`, `vertex`, `azure_openai` — SDK-shaped auth (SigV4/GCP/OAuth token
  exchange); a Bearer-injecting proxy cannot speak them by construction.
- `opencode` (zen) — auth-store merge path, `shouldSkipRelay`'s concern (US-72.4
  verified orthogonal).

K1 full coverage for these kinds is a US-72.5 flip-gate question, flagged in the PR.

### D6 — Staleness classification + escalation predicate (§4.2, controller-homed)

Recomputed fresh each pass, cause → class:

| Cause (observable now) | Class | Escalates? |
|---|---|---|
| pub Secret unparseable/missing (seal-time parse fails loudly) | corruption-adjacent (`PubUnreadable`) | no (router-side watch assert self-heals keypair corruption; controller cannot compare generations against an unparseable pub) |
| agentd degrade `credential_stale` (the router's resolve-failure code — wrong/corrupt keypair lineage, §4.2's one escalating cause; live emission lands with US-72.4's degrade codes) | corruption | **yes** (with lineage + anti-storm) |
| handoff token past expiry / mint failure | token-expiry | no (renewal owns) |
| provider unbound this pass (envelope deleted, D2) | revocation | no (terminal by design) |
| batch/#852 deferral, `relay_unreachable`, unknown codes, spawned_rev behind staged revision | delivery | no |

Lineage-intact conjunct (the `spawned_rev`-class terminal signal, NOT the batch-apply
anchor): all desired envelopes present AND
`ws.Status.SecretsDelivery.SpawnedRev == current staged revision`. Pre-US-72.4 pods
never satisfy it (their spawned_rev tracks the raw batch) — so escalation is
structurally suppressed until the builder swap makes the signal real; the predicate and
its tests land here, per the story.

Anti-storm bound: cluster-global (rotations are cluster-wide), persisted as the
`llmsafespaces.dev/last-rotate-escalation` annotation on the `llm-relay-mint-key`
Secret — shared across workspaces and across controller restarts; escalation spaced
≥10m (the prior-key retention default).

### D7 — Condition lifecycle choices

`CredentialStale(revocation-class)` is raised on the pass that performs the envelope
deletion and clears on the next clean pass (one-pass visibility; the durable post-72.4
signal is the agentd `credential_stale` degrade feeding the same condition). Staging
failures are never fatal to phase progression pre-flip (the raw-key path still
delivers): they surface as conditions + events + requeue. `CredentialRejected` is set
from router rejection telemetry (degrade codes `scope_violation` /
`sanitization_refused` / `quota_exceeded`; emission lands with US-72.4) + a Warning
event, per §4.7 "violations are CredentialRejected-class events, never silent".

### D8 — Persistence of staging state

Per-workspace annotations on the Workspace (`llmsafespaces.dev/relay-sealed-generation`,
`…-staged-revision`, `…-staged-providers`), written only on change (no reconcile
hot-loop); global rotate-escalation timestamp on the mint-key Secret (D6). A lost
sealed-generation annotation degrades safely to a one-time re-seal that converges
(annotation rewritten on that pass) — no fallback derivation is needed or implemented;
the controller holds no llm-relay reads to derive it from (see D-RBAC below).

---

## Work Completed

### `pkg/secrets` — credential-source seam + endpoint table

- `ResolveLLMProviders` (injection.go): same rows / dedup / decrypt matrix / allowlist as the batch builder, audited per reveal (`internal_llm_providers_reveal`); the staged set is by construction the batch's provider set.
- `ProviderDefaultBaseURL` / `ProviderRelayStageable` (provider_endpoints.go, new): the kind→upstream table + stageable kind set.
- Envelope/mint/pub Secret contract constants single-sourced in `staging_provider.go` (`RelayEnv*`, `RelayPubSecretName`, `RelayMintKeyName`, …) and the router's `byo_cache.go`/`byo_keys.go`/`byo_run.go` now reference them (constants only, no behavior change).

### API — internal llm-providers endpoint

- `api/internal/handlers/internal_llm_providers.go`: `GET /api/v1/internal/workspaces/:workspaceID/llm-providers?ownerUserID=` — the org-status auth pattern (constant-time X-Internal-Token, fail-closed 403/401), generic 500 bodies (no store detail), registered in router.go, constructed in app.go.

### Controller — staging core (`controller/internal/workspace/staging*.go`)

- `staging.go`: the full pass — desired-set split (stageable vs skipped-with-reason), envelope upsert under the router informer contract, revocation (delete + same-pass `UnregisterStagedKey`, one-pass revocation-stale + event), token mint/renew (TTL/2), handoff Secret (owner-ref'd), §4.2 terminator, §4.6 conditions, change-gated two-phase persistence (status write, then re-applied metadata write — the fake-client copy-back semantics forced this ordering; proven against a real API server by the envtest matrix).
- `staging_client.go`: `CachedLLMProviderSource` (60s success-only cache — NO stale-serve, unlike org-status) + `HTTPRelayRouterClient` (mint/rotate, Bearer mint key read from the controller-created Secret).
- `staging_guard.go`: `ValidateRelayStagingFlags` + `ProbeRelayRouter` + `ValidateRelayStagingStartup` (namespace present, pub readable+parseable — also proves the get-on-pub RBAC — mint key created).
- Hooks: `handleCreating` (before pod build) + `handleActive` (renewal/DR); `handleTerminating` runs `relayDeleteEnvelopes` (cross-namespace Secrets get no owner-ref GC).
- `workspace_types.go`: `CredentialsStaged` / `CredentialStale` / `CredentialRejected` condition types + the §4.6 reason vocabulary. Conditions are free-form typed strings — no CRD schema change.

### Controller — flags + wiring

- `controller/main.go`: `--relay-only-key-delivery` (default false), `--llm-relay-router-url`, `--llm-relay-namespace` (default llm-relay), `--relay-token-ttl` (default 24h, clamped 1s..7d).
- `controller/internal/controller/controller.go`: `SetupRelayStaging` (construct + guard, the SetupRelayController pattern) + the `RelayStaging` reconciler field.

### Helm

- `controller-deployment.yaml`: the four args under `relayOnlyKeyDelivery.enabled`, with a RENDER-FAIL guard when enabled without `workspaceRouterURL` (install-time half of the fail-loud gate; the runtime probe is the other half).
- `helm/llm_relay_staging_chart_test.go`: flag-off inertness, wired args, namespace/TTL threading, guard-fails-render.

### Branch mechanics

- Rebased `--onto origin/main` after #1432 merged (squash a471fdb0); COORDINATE.md union-resolved (kept main's rows, added the US-72.3 active claim); force-pushed (no PR was open). The post-rewrite hook assigned worklog 0988.

---

## Key Decisions

D1–D8 above.

---

## Blockers

None.

---

## Tests Run

- `go test ./controller/internal/workspace/ -count=1` — ok (full existing suite + the new staging suites: stage/unbind/rotate legs, `pub_sealtime_generation_validated`, `dr_window_reconcile_terminates` incl. anti-storm + the full suppression matrix, `mixed_fleet_batches`, flag-off inertness, guard matrix, HTTP-client seams).
- `go test ./pkg/secrets/ -count=1` — ok (endpoint table + ResolveLLMProviders parity/dedup/failure legs).
- `go test ./api/internal/handlers/ -run TestInternalLLMProviders -count=1` — ok.
- `go test ./helm/ -count=1` — ok (relay staging chart tests + the existing chart suite; helm v3.16.4 on PATH).
- `go build ./...` — ok. `go vet` (incl. `-tags envtest`) — clean.
- `golangci-lint run` scoped to every changed package — 0 issues (v2.13.2).
- envtest conditions matrix (`staging_envtest_test.go`, tag `envtest`) — compiles + vets here; KUBEBUILDER_ASSETS unavailable in this environment, CI's envtest workflow owns the run (same situation as the existing envtest suites).

## Next Steps

- **US-72.4** consumes the handoff Secret (`workspace-relay-<ws>`, data key `handoff`, shape above): batch swap apiKey=token / baseURL=routerPath, `spawned_rev` starts tracking the staged revision (which makes the lineage conjunct live), agentd degrade codes `credential_stale` / `token_expired` / `relay_unreachable` + router reject codes feed the classification landed here.
- **US-72.5** flip prep: decide K1 coverage for the non-stageable kinds (bedrock/vertex/azure_openai — SDK-shaped auth the proxy cannot front; currently skipped + surfaced) and wire KMS-mode controller construction if a KMS deployment is planned before the flip.

## Files Modified

- `pkg/secrets/provider_endpoints.go` (new) + `provider_endpoints_test.go` (new)
- `pkg/secrets/resolve_providers_test.go` (new)
- `pkg/secrets/injection.go` (ResolveLLMProviders)
- `pkg/secrets/staging_provider.go` (relay Secret-contract constants)
- `api/internal/handlers/internal_llm_providers.go` (new) + `internal_llm_providers_test.go` (new)
- `api/internal/server/router.go`, `api/internal/app/app.go` (endpoint wiring)
- `controller/internal/workspace/staging.go`, `staging_client.go`, `staging_guard.go` (new)
- `controller/internal/workspace/staging_test.go`, `staging_dr_test.go`, `staging_guard_client_test.go`, `staging_envtest_test.go` (new)
- `controller/internal/workspace/reconciler.go` (RelayStaging field), `phase_active.go`, `phase_creating.go`, `phase_terminating.go` (hooks)
- `pkg/apis/llmsafespaces/v1/workspace_types.go` (conditions + reasons)
- `controller/main.go` (flags), `controller/internal/controller/controller.go` (SetupRelayStaging + field wiring)
- `cmd/relay-router/byo_cache.go`, `byo_keys.go`, `byo_run.go` (constants single-sourced — no behavior change)
- `helm/templates/controller-deployment.yaml`, `helm/llm_relay_staging_chart_test.go` (new)
- `COORDINATE.md`, this worklog

---

## Addendum — review iteration 2 (2026-09-18, same session)

The AI reviewer requested changes with three validated findings; all fixed with reproducing tests, plus the non-blocking items:

- **R1 (blocker — RBAC↔code↔design contradiction):** settled by RELOCATING the reads rather than broadening the Role: the envelope Secrets are now written **blind** (delete-by-derived-name + create; no Get, no Update-with-read), all diffing decisions (re-seal, revocation, §4.9 unregister, value rotation) read bookkeeping persisted in Workspace annotations (`llmsafespaces.dev/relay-staged-providers`: per-slug keyDigest/keyID/modelsDigest/redactionID); the llm-relay `relayExistingEnvelopes` LIST is GONE; the startup guard's cluster-scoped Namespace GET is GONE (the pub read doubles as the namespace/RBAC proof — pinned by test). The Role gains exactly two NAME-SCOPED grants (get on `llm-relay-hpke-pub` + `llm-relay-mint-key`; update on `llm-relay-mint-key` for the anti-storm timestamp) — the private-key Secret stays unreadable and llm-relay is never listed (a list would return the keypair payload). Design 0058 §4.3 deviation note amended; the US-72.2 chart pin updated to pin the NEW contract (blind-write general rule, name-scoped get/update, no list, never `llm-relay-hpke-key`).
- **R2 (blocker — credential VALUE rotation never re-sealed):** the per-slug `keyDigest` (sha256[:12], non-reversible fingerprint — never the plaintext) in the staged-providers annotation forces a re-seal on any key-value change; repro test `TestStaging_CredentialValueRotationReseals` proves the envelope resolves to the NEW key after the change (the reviewer's probe showed the old key surviving pre-fix).
- **R3 (transient mint failure dropped a valid token):** `relayMintTokens` retains the unexpired cached entry on a failed renewal mint (the handoff Secret is the only token store); only a fully-expired or keyID-stale token is dropped, loudly. Repro: `TestStaging_MintFailureRetainsUnexpiredToken` (uses the previously-unused `mintErr` hook).
- **Non-blocking, all fixed:** revocation delete failure is now fail-closed (slug stays in the revocation set, retried next pass, surfaced) — the List-swallowing issue disappeared with the List itself; `relayMarkStale` no longer clears `CredentialStale` during staging-infra outages; a failed rotate emits a `RelayRotateEscalationFailed` Warning event (the 10m budget is still consumed — deliberate anti-storm — but now visibly); `ReasonStaleEnvelopeMissing` is emitted (seal failure for a bound provider = envelope absent); `fetch` escapes `workspaceID`/`ownerUserID`; **class ordering: revocation wins over a co-present corruption-class degrade** (the degrade is most plausibly about the just-revoked provider; §4.2 forbids revocation-class escalation) — combination test added; worklog D1 cache drift fixed (60s); style nits (orphaned renew-margin comment, import-keeper var, min/hasPrefix helpers → builtins/strings).

**Steady-state note:** with annotation-driven diffing, a no-change pass performs zero llm-relay API calls and zero workspace writes (pinned by `TestStaging_SteadyStateZeroWrites`; a controller restart re-seals each envelope once — blind delete+create, idempotent).

---

## Addendum — review iteration 3 (2026-09-18, same session)

- **R3-1 (blocker — cached-client llm-relay reads):** `RelayStagingConfig.APIReader` added — every llm-relay read (pub, mint-key) goes through the DIRECT reader (`relayReader()`; wired to `mgr.GetAPIReader()` in `SetupRelayStaging`), never the cached reconciler client (controller-runtime's multi-namespace cache cannot serve out-of-scope namespaces and an in-scope informer would need LIST+WATCH the Role withholds). Writes stay on the reconciler client (they bypass the cache). Pinned by `TestStaging_LLMRelayReadsGoThroughAPIReader`: the pass completes with a reconciler client whose llm-relay reads fail cache-style — the production namespace-scope topology, simulated.
- **R3-2 (non-convergent persist gate):** `annotationsDirty` now includes the `relay-staged-providers` annotation — the reviewer's churn loop (annotation lost → re-seal every 15s for up to TTL/2 with the rebuilt annotation never persisted) is closed. Pinned by `TestStaging_StagedProvidersAnnotationLossConverges` (wipe only that annotation → re-seal once, PERSIST the rebuild, next pass zero-write).
- **R3-3 (RBAC-denied KEK probe):** the `llm-relay-kek` GET is gone (it was Forbidden in-cluster, dead code for the D4 diagnostic). The pub-not-found message now names BOTH possibilities (router unbootstrapped OR a KMS-mode deployment where controller staging is not yet wired) without reading anything; the design amendment note documents the indistinguishability as trade-off (iii), resolved with US-72.5's KMS wiring.
- **R3-4 (revocation delete-failure misreport + no requeue):** `relayRevokeUndesired` now separates completed (`revoked`) from `pending` revocations; the pass PROPAGATES an error (caller requeues), the pending slugs stay in the staged bookkeeping for the retry, and the condition message says "revocation INCOMPLETE … retained + retrying" instead of claiming deletion. `relayStaleClass` treats pending revocations as revocation-class. Pinned by `TestStaging_RevocationDeleteFailureRetriesAndReports` (failed delete → error + honest message + retained bookkeeping; healed delete → completes).
- **R3-5 (design trade-offs):** the §4.3 amendment note now states the blind-write durability window (torn delete/create or externally-deleted envelope is unrepaired until a decision input moves — rotation, credential change, §4.2 escalation; needs US-72.4's degrade feed), the 48-bit fingerprint caveat, and the KMS-indistinguishability trade-off. The superseded US-72.2 paragraph is marked as superseded instead of silently contradicting the amendment.

Tests: full `controller/internal/workspace` suite green; golangci-lint 0 issues; gofmt/goimports clean.

---

## Addendum — review iteration 4 (2026-09-18, same session)

- **R4-1 (blocker — data race / cross-workspace contamination on `relayPendingRevocations`):** the reconciler field is GONE; pending revocations are parameter-threaded (`relayRevokeUndesired → (revoked, pending, err)`; `classifyRelayStale(ws, revoked, pending)`; `relayEvaluateStale(ws, revoked, pending, tokensFailed)`) — all consumers synchronous within one pass, no shared state. Pinned by `TestStaging_ConcurrentPassesRaceFree` (two workspaces' passes concurrently under `-race`).
- **R4-2 (blocker — false-green convergence test):** the annotation loss is now STORE-LEVEL (client Update + refetch), and I red-verified the test FAILS with the persist-gate disjunct removed and passes at head — a real regression pin.
- **R4-3 (message-only condition updates unpersisted):** `relayCondSig` includes the message, so a reason-preserving message change (e.g. INCOMPLETE → completed wording) persists.
- **R4-4 (early-return overstatement):** the revokeErr path now persists the updated staged bookkeeping too (completed revocations leave the staged set immediately; only the pending ones retry).
- **R4-5 (PR body decision 4 vs code):** PR body updated — KMS-mode without pub now surfaces as CredentialsStaged=False/PubUnreadable naming both possibilities (the llm-relay-kek probe was RBAC-dead code); design trade-off (iii) is the operative contract.
- **Missing-test items:** the APIReader is now REQUIRED at construction (`NewRelayStagingConfig` errors on nil — pinned by test), so the production wiring line cannot be silently dropped; the concurrency pin above; the store-level convergence test above.
- **Robustness:** termination-time envelope delete failure now emits `RelayEnvelopeOrphaned` (Warning) — design trade-off (v); the revocation-requeue starvation is documented as trade-off (iv). Style: the dead `handoff` param on relayEvaluateStale removed with the signature change.

---

## Addendum — review iteration 5→6 (2026-09-18, same session)

- **R5-1 (worklog D4 vs code):** D4 now describes the actual mechanism — absent OR shape-unreadable pub → `CredentialsStaged=False`/`PubUnreadable` naming both possibilities (unbootstrapped router OR unwired KMS mode); the controller holds no `llm-relay-kek` get, so it deliberately cannot distinguish them (design trade-off iii; the r3 probe removal).
- **R5-2 (message-persistence unpinned):** `TestStaging_MessageOnlyConditionChangePersists` — same status/reason, message-only change (the degrade detail) persists to the stored condition in the same pass.
- **R5-3 (rbac.scope=cluster weakens the no-read guarantee):** render-time `fail` when `relayOnlyKeyDelivery.enabled` with `rbac.scope=cluster` (the cluster scope's pre-existing cluster-wide Secret get/list/watch would replicate the keypair Secret into controller memory; the llm-relay Role cannot subtract). Chart test `TestRelayStaging_ClusterScopeRefusesRender` pins both the refusal and that the default namespace scope renders.
- **R5-note:** token retained through a mint failure keeps its previous ModelAllowlist/BaseURL scope until the next successful mint (≤TTL/2) — added as design trade-off (vi).
